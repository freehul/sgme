# SGME Backlog（需求池）

> 本文件是 SGME 项目的**锚文档**（Backlog）——记录所有需求（Epic/Story）与问题（Task/Bug），持续存在、持续更新。
> 术语对齐 GitHub Issue 体系：Epic → Story → Task/Bug，全部 Task 满足 Acceptance Criteria = Story 完成。
> 设计文档依附于本文件（见「设计文档索引」）；开发完成更新状态（**标记，不删除**）。
> 未关闭的条目 = 当前任务池。

---

## Milestone 规划

| 里程碑 | 目标 | 包含 |
|--------|------|------|
| v0.7 | 已发布 | ST-1 核心链路、ST-2 三层检索、ST-3 注入、ST-4 降级链 |
| **v0.8** | 2026-08-13（建议） | ST-9/10/11/14/15/16、T-7/8/12/13/14、接口层缺口（F1 浏览端点 + 模板管理 API + L0 滑窗） |
| **v1.0** | 2026-08-18 | ST-7 WebUI（含原 ST-17 创意池管理 UI）、ST-8 hook 落地、Bug 修复 |

---

## Epic

### EP-1 记忆引擎核心（In Progress）
> 把 Agent 会话提炼为标签化记忆，按模式模板注入画像，检索可溯源。

| 编号 | 类型 | 标题 | 状态 | 版本 | AC |
|------|------|------|------|------|----|
| ST-1 | Story | 会话 L0 捕获（append 落盘 + 增量段） | ✅ 已解决 | v0.4 | 会话可落盘、重复 append 追加、增量段触发重新提炼 |
| ST-2 | Story | L1 记忆提炼（LLM 标签化 + 维度归一化） | ✅ 已解决 | v0.4 | 提炼产出标签化记忆、维度归一化、Supersession 归档 |
| ST-3 | Story | L1.5 冲突检测 + L2 场景生成 | ✅ 已解决 | v0.6 | 冲突识别、场景生成、关联记忆 |
| ST-4 | Story | 三层检索（BM25 + 向量 + RRF） | ✅ 已解决 | v0.4 | 检索命中、溯源可达 |
| ST-5 | Story | 模板注入（零 LLM） | ✅ 已解决 | v0.4 | daily/custom_filter 模式、TTL 过滤、预算截断 |
| ST-6 | Story | LLM 降级链 + 模型白名单 | ✅ 已解决 | v0.7 | 主/备/兜底切换、白名单拒绝加载 |

### EP-2 产品化与运营（Not Started）
> 让 SGME 成为可部署、可管理、可持续运营的产品。

| 编号 | 类型 | 标题 | 状态 | 版本 | AC |
|------|------|------|------|------|----|
| ST-7 | Story | WebUI 管理面板（4 导航 22 视图，含创意池管理 UI——原 ST-17 并入） | ✅ 已解决 | v1.0 | 4 导航项全部可用、22 视图齐全、创意池管理页可用（列表/检索/编辑/标记/升格，路由 /ideas）；**代码已齐（2026-08-13 盘点）：15 路由 + 设置 9 标签页 + 11 api client 全部实现，`npm run build` 通过；待浏览器逐视图验收后标 ✅**。**验收完成（2026-08-20 无头验收，见 SGME-WebUI验收报告-v0.1.md）**：27 路由（26 子路由 + 根 /）全核对；29 视图文件全存在（18 路由组件 + 9 设置标签页 + ConfigTab/ModulesView 未挂载遗留 2 个仅备注）；12 api client 全被引用；4 导航实际命名 总览/记忆闭环/创意与需求/系统管理；`npm run build` 通过（vite 5.4.21，111 modules，937ms，exit 0）；27/27 ✓ 0 ✗ |
| ST-8 | Story | MEMORY.md 写入纪律 + pre_llm_call hook 场景注入 | ✅ 已解决 | v1.0 | ①写入纪律已定（写前三问）②hook 已停用（2026-08-09 方案A：SGME 插件 prefetch 已覆盖场景检索）③**设计决策已定：hook 内容由 SGME 设置确定，方向 1——内容存 SGME（templates/config），hook 经 HTTP 拉取注入，SCSM 可改**（2026-08-09 决策，见 DECISIONS）④**实施计划（2026-08-11 用户定）：hook 落地先接 SGME 默认模板（templates/*.yaml）跑通，待 ST-7 WebUI 模板管理完成后做注入测试**。**注入测试通过（2026-08-20，见 SGME-ST8注入测试报告-v0.1.md）**：GET /v1/admin/templates 返回 4 模板（daily/coding/work/full）结构完整；POST /v1/inject mode=daily 返回 200，4 blocks（tier0 画像摘要 + 3 sections 真实记忆数据），stats/tier0 字段齐全；daily 维度覆盖 identity/family/habits/status/focus（sections 声明，goals 属 work/full 模板）；注入零副作用（不写库不调 LLM），hook 可经 HTTP 拉取注入 |
| ST-9 | Story | L0 原文读取 API（远程 Agent 读原始会话） | ✅ 已解决 | v0.8 | HTTP 按 file_id 读原文 | L0 原文读取 API（admin+agent 双鉴权，2026-08-10 与 F1 合并实施） |
| ST-10 | Story | Dream 夜间整理（cron 抽取→判决→生命周期→日报） | ✅ 已解决 | v0.8 | 定时自动执行、失败有预案 | Dream 夜间整理四步编排+定时器+日报（2026-08-10） |
| ST-11 | Story | skills-hub copy 模式真实同步 | ✅ 已解决 | v0.8 | copy 模式跨机同步可用 | skills-hub copy 真实同步 git bare repo（2026-08-10） |
| ST-12 | Story | Docker 部署方案 | ✅ 已解决 | v1.0 | NAS 一键部署——2026-08-20 deploy.sh（构建→导出→传输→导入→启动→验证全流程），B88 |
| ST-13 | Story | D3 知识图谱可视化 | ✅ 已解决 | v1.0 | 记忆/场景关系可视化——2026-08-20 GET /v1/admin/graph + GraphView.vue（D3 force），B95 |
| ST-14 | Story | 创意池（ideas 维度 + 提炼提示词方向） | ✅ 已解决 | v0.8 | 自动捕获对话思维闪光，长期保存（无 TTL），带溯源 trace；设计见 SGME-创意池与需求池设计-v0.1 | 创意池（ideas 维度+备注+升格，2026-08-10） |
| ST-15 | Story | 需求池（demands 维度 + 项目关联） | ✅ 已解决 | v0.8 | 跨项目宽泛需求，状态流转（未立项→已立项→已解决），与项目注册表双向关联 | 需求池 demands 表+状态流转（2026-08-10） |
| ST-16 | Story | 项目注册表（project_meta） | ✅ 已解决 | v0.8 | 项目名/路径/git 仓库/最近活跃/当前里程碑；跨项目检索基础 | 项目注册表 project_meta（2026-08-10） |
| ST-17 | Story | 创意池管理 UI | ✅ 已并入 ST-7（2026-08-09） | — | 列表/检索/编辑/备注/标记/升格（并入 ST-7 WebUI，路由 /ideas） |
| ST-19 | Story | 检索术语别名归一化（术语漂移治理） | ✅ 已解决 | v1.0 | 检索层术语别名映射（如 daemon/SGME Server → SGME Gateway），查询与索引双端归一化，防旧术语记忆漂移；触发词：SGME 改名 Gateway 后旧名记忆召回错位（2026-08-11）→ 2026-08-11 实施：registry/term_aliases.yaml + 查询扩展归一化（PR#4，无回归双召回；索引端评估不做） |
| ST-20 | Story | skills_hub remote.source 环境变量覆盖（发布脱敏配套） | ✅ 已解决 | v1.0 | 配置支持 env 覆盖 source（如 SGME_SKILLS_HUB_REMOTE），config/sgme.yaml 默认占位符、部署时 env 注入真实地址，避免仓库内硬编码 NAS 地址；触发词：2026-08-11 GitHub 发布前脱敏（当前方案=skip-worktree 本地保护，长期应迁移至此）→ 2026-08-11 实施：ENV_OVERRIDES 通用机制（PR#5，env > yaml > 默认；落盘保持占位符防泄漏；本地 NAS 配置 skip-worktree 保留） |
| ST-21 | Story | 1.0 发布前文档与历史重构 | ✅ 已解决 | v1.0 | ①架构 v0.9 文档 127 处版本引用清理（v0.6/v0.7 对照、变更摘要、冲突点附录、ST/T 编号）→ 基于现有代码逻辑的"当前态"全新文档，无历史羁绊；②git 全历史敏感词清理（filter-repo 替换 + force push GitHub/NAS，2026-08-11 用户定：趁 1.0 上传时一起处理，私有测试期暂缓）；触发词：2026-08-11 用户定，1.0 发布前执行；另含 README/runbook/评测文档同类清理；①已完成（2026-08-11 PR#12：架构 2691→2205 行当前态，127 处版本痕迹全清）；②git 历史 filter-repo 清理——**2026-08-20 审计结论：无需执行**（git log 全历史扫描无 .env 提交、无真实 key（sk- 命中为脱敏修复提交标题）、config 模板全占位符、.env.example 空值模板；2026-08-11 dce02f7 已做 append 脱敏） |
| ST-22 | Story | 新手体验修复（onboarding audit 18 问题）+ MCP 补齐 + 鉴权加固 | ✅ 已解决 | v1.0 | 见 docs/audit/SGME-新手体验审计-2026-08-11.md（Reasonix agent 视角，笔记本实测）：优先项=①runbook 对齐现状（LLM 主链 deepseek/向量火山方舟，§4.2 旧模型名 qwythos 修正）+ **LLM 配置表述提供商无关化**（2026-08-11 用户定：文档不指明品牌，任选 OpenAI 兼容提供商，providers.yaml 为真相源，deepseek 降为示例之一；含 config/llm.yaml 默认链加"可替换为任何 OpenAI 兼容提供商"注释）②health 加 vector 可用性 + inject 空 block 提示（✅ 2026-08-11 PR#6：health 响应含 vector 字段 + inject 空结果中文引导）③README 补默认 dev key 与 L0 content 格式④错误信息引导（403/422 统一中文 ERR_*）（✅ 2026-08-11 PR#6：422→ERR_INVALID_ARGS + 兜底映射 + 鉴权引导文案）⑤VOLC_API_KEY 文档化⑥**MCP 补齐 9→12 工具**（✅ 2026-08-11 PR#7：9→13 含 agent_onboarding，批量提炼 refine_batch、记忆拒绝 memory_reject、提炼状态 refine_status，2026-08-11 用户定：通用 agent 走 MCP 完成日常一切）⑦**鉴权 onboarding**（README 文档化 register 签发流程 + 测试补 register 验证——2026-08-11 已实测闭环通过：签发/使用/边界/持久化/吊销全链路 OK，剩文档化）⑧**默认 key localhost 限制**（✅ 2026-08-11 PR#6：默认 dev key 非 localhost 来源请求 403 提示换 key，自定义 key 不受限；2026-08-11 用户定：仓库公开=默认 key 公开，0.0.0.0 部署时任何人可 admin）；触发词：2026-08-11 笔记本全链路验证完成；全部完成（2026-08-11）：①runbook 提供商无关化 ③README 待并发会话落定 ⑤VOLC 文档化 ⑦鉴权 onboarding 文档化（PR#11 文档包） |
| ST-23 | Story | Agent 自我适配机制（onboarding 指引 + 每轮 L0 捕获） | ✅ 已解决 | v1.0 | ①适配载体三件套：MCP 服务端 agent_onboarding 工具（连接即发现，self-serve）+ README「给 AI Agent 的话」段落 + docs/agent-onboarding.md 完整指引；指引含**LLM 限流与批量提炼策略**（2026-08-11 Trae 全量提炼实测撞 429：批量≥20 文件分批节流+批间 30-60s、429 失败不立即重试交 batch_scan 兜底、异步触发、进度查询法、预期管理）；②三档适配：hooks 型（检测 SessionEnd/Stop 配自动提炼 hook，Hermes/Reasonix/Claude Code）、自律型（无 hooks → 指引要求会话收尾主动调 refine/trigger_async，Trae/ZCode）、保底型（服务端 batch_scan + refine_on_append 不依赖 agent 自觉）；③**每轮 L0 捕获行为要求**（2026-08-11 用户修正定案）：agent 每轮对话结束 append 当前轮次（纯落盘零 LLM 成本，崩溃不丢），会话结束 refine（完整上下文）；Hermes/Reasonix 适配器从 SessionEnd 一次性导出改为每轮 append + 结束补最后一轮；**tool 消息导出重复治理**（2026-08-11 抽查实锤：621 文件 996 块同时间戳 tool 输出重复 3-6 次，导出层同一事件多次写入——适配器导出去重，防提炼噪音与 token 浪费）；④就绪检查：LLM/数据源缺失时 agent 主动提醒用户配置，**提醒表述提供商无关**（2026-08-11 用户定：报"LLM 未配置——请配置你所用提供商的 key（见 providers.yaml）"，不指明品牌；环境清单同理：教配置方法与查找路径，品牌由用户选）；⑤MCP 协议兼容：Trae 自定义通知 notifications/trae/session_stop 宽松处理（2026-08-11 实测：官方 SDK 严格枚举拒绝，日志刷屏，Trae 会话结束事件收不到）；⑥**LLM 429 限流硬处理**（2026-08-11 实锤：429 不在 fallback_on 列表→直接提炼失败无退避，批量时连环撞限流；方案=rate_limit 入 fallback_on + 指数退避重试 + LLM 调用层节流器，根治不依赖 agent 自觉）；⑦**服务发现机制**（2026-08-11 用户批评修正：新 agent 找不到 SGME 安装位置=onboarding 缺发现机制，人肉解决不可扩展；方案三步=本机探测 localhost:9910/v1/health 零配置发现→失败读安装清单 ~/.sgme/install.json（地址/端口/key 引用，本机不泄露目录结构等个人信息）→仍失败则 agent 就绪报告"SGME 未发现"并提醒用户安装/启动；项目目录语义=CLI --dir 指定、Desktop 打开项目文件夹，指引写明由 agent 执行）；触发词：2026-08-11 Trae MCP 直连实测 + 每轮 L0 保险讨论；**已完成（2026-08-11）**：①agent_onboarding 工具落地（PR#7，连接即发现）；README「给 AI Agent 的话」已补（2026-08-12：中英文 README 新增「接入你的 AI——给 AI Agent 的话」章节，含提示词模板 + 身份文件放置位置对照）②保底型 batch_scan 实现（PR#8，常驻定时器 + Dream 共享锁）③Hermes 适配器每轮增量捕获 + tool 去重 + 修复固定 started_at 失效（PR#9，5 测试）+ Reasonix SessionEnd 增量导出（PR#10，游标持久化 + tool 去重，15 测试）⑤Trae 通知宽容处理（PR#7）⑥429 硬处理（PR#1，rate_limit 入 fallback_on + 退避 + 节流器）|
| ST-24 | Story | Docker 化 + NAS 真实部署验收 | ✅ 已解决 | v1.0 | ①Dockerfile + docker-compose.yml（venv 打包/端口 9910+9913/数据卷 data+raw/env 注入）②NAS 真实部署验收（Linux/Docker 形态：路径权限/常驻/环境变量/备份 remote_dir 挂载/数据迁移）；触发词：2026-08-11 用户定——接入验证（协议/鉴权/共享记忆）本机跨机已等价覆盖，但部署形态验证（NAS 最终使用形态）需真实部署，与 1.0 转公开/tag 一起作为发布验收；当前项目无任何 Docker 资产（已核实），Docker 化是前置工作量。**已完成（2026-08-14，B58）**：①Dockerfile（python:3.11-slim、程序资源内置 /app、SGME_HOME=/data 落挂载卷、VOLUME /data、EXPOSE 9910+9913）+ docker-compose.yml（build+healthcheck+restart:unless-stopped+env_file docker.env+数据卷 sgme-data）+ .dockerignore（密钥/数据/开发产物排除）+ .env.example 密钥模板；②笔记本 Docker Desktop 实测：镜像 sgme:1.0.0b1（463MB/tar 109MB）构建成功、容器 health 200、llm.available=true、append→refine→search 端到端通过；③NAS（群晖 DSM + Docker 29.1.2）实测：docker load 成功、bind mount /vol1/1000/Docker/sgme/data、容器 (healthy)、health ok、端到端检索命中「家目录部署在群晖 NAS」；部署文档 docs/deployment-docker.md |
| ST-25 | Story | Care Engine 主动关怀引擎（情绪洞察 + 待办提醒 + 主动推送） | ✅ 已解决 | — | AC：①消费 signal_events 信号（memory_updated 等——2026-08-13 盘点：2134 条事件已发、consumed_at 全空，**信号总线就绪但无消费者**）②情感分析 + 待办推导合并为一个模块（2026-07-06 用户确认方向正确），消费情绪波动、工作计划、作息节奏等信号产出关怀动作 ③关怀动作类型：情绪主动关心 / 工作计划提醒 / 目标矛盾检测（想省钱却重复订阅）/ 过度工作预警（2026-08-11 需求）④主动推送通道（Hermes 等 Agent 触达）⑤与 Dream 定时器协同（定时扫描而非常驻轮询）；触发词：2026-08-13 用户盘点——主动关怀是个人记忆引擎核心诉求，Care Engine 立项；设计文档：docs/design/SGME-CareEngine设计-v0.1.md。**已完成（2026-08-13，T-35~T-38 全部 ✅）**：①角色层（T-35：CC V2 兼容角色卡 + persona 物化）②关怀信号（T-36：care_todo_due/care_mood/care_overwork/care_daily 零 LLM 推导 + scan/list/consume 端点）③装配（T-37：换皮不换芯）④消费方（T-38：scripts/care_consumer.py cron/heartbeat 双轨）；架构定位=SGME 只发信号/存记忆/供角色数据，关怀决策与触达在消费方 agent（Hermes cron 注册待用户确认，见 B45）；调研背书（2026-08-13 三子代理：记忆层与动作层分离是行业铁律） |

---

## Task / Bug 池

| 编号 | 类型 | 标题 | 归属 Story | 状态 | 版本 | 备注 |
|------|------|------|-----------|------|------|------|
| T-1 | Bug | 归档 FK 崩溃（memory_stats 清理链缺失） | ST-1 | ✅ 已解决 | v0.7 | P0 |
| T-2 | Bug | 维度注册表 YAML/DB 重启互相覆盖 | ST-2 | ✅ 已解决 | v0.7 | 裁决：YAML=种子，DB=真相 |
| T-3 | Bug | TTL 字符串比较错误 | ST-5 | ✅ 已解决 | v0.7 | |
| T-4 | Task | validate_models 白名单接线 | ST-6 | ✅ 已解决 | v0.7 | 死代码接线 |
| T-5 | Task | record_inject 接线（memory_stats 统计） | ST-5 | ✅ 已解决 | v0.7 | |
| T-6 | Task | 候选池整池硬截断修复（铁律 #7） | ST-3 | ✅ 已解决 | v0.7 | |
| T-7 | Task | 限流实现（契约承诺 120 req/min） | ✅ 已解决 | v0.8 | — | 滑动窗口限流 120 req/min（2026-08-10） | → v0.8（P1） |
| T-8 | Task | 4 条链路补 operations 层 | ✅ 已解决 | v0.8 | — | registry/prompts/backup/events 四链路 operations 化（2026-08-10） | → v0.8（P1） |
| T-9 | Bug | 非 data 层直查 SQL | EP-1 | ✅ 已解决 | v1.0 | 2026-08-20 health/signal 直查收口 stats_dao/signal_dao（wiki 已走 DAO），B91 |
| T-10 | Bug | 评测框架文件清单与实现不符 | EP-1 | ✅ 已解决 | — | |
| T-11 | Bug | dimensions.boundaries 字段静默丢弃 | ST-2 | ✅ 已解决 | v1.0 | 2026-08-20 YAML→DB→L1 提示词全链路保留，B92 | |
| T-12 | Task | 提炼健康自检停摆告警 | ✅ 已解决 | v0.8 | — | 停摆告警 seq 水位推进+anomaly_warn（2026-08-10） | → v0.8（P3） |
| T-13 | Task | ingest 任务持久化（wiki 重启丢状态） | ✅ 已解决 | v0.8 | — | ingest_tasks 持久化+重启恢复（2026-08-10） | → v0.8（P3） |
| T-14 | Task | 关系类型扩充（wiki_links 原 4 种 → 24 种） | ✅ 已解决 | v0.8 | — | relations.yaml 26 种注册表化（2026-08-10） |
| T-15 | Task | F1 浏览类五端点（/v1/admin/*） | ✅ 已解决 | v0.8 | — | F1 浏览五端点（2026-08-10） |
| T-16 | Task | 模板管理 API（/v1/admin/templates） | ✅ 已解决 | v0.8 | — | 模板管理 API（2026-08-10） |
| T-17 | Bug | 向量搜索路径未过滤 rejected 记忆（召回过时记忆根因） | ✅ 已解决 | v0.8 | — | sqlite-vec + numpy 两路径补 `status != 'rejected'`（2026-08-11，触发词：AIXM 过时记忆被召回） |
| ST-18 | Story | 项目替代联动失效：提炼出「X 已被 Y 替代」时自动标记 X 相关记忆过时 | ✅ 已解决 | v1.0 | 新记忆声明替代关系时，旧主体相关记忆自动标记（rejected/archived），防过时记忆长期存活（2026-08-11 用户报 AIXM 案例）→ 2026-08-11 实施：L1 提示词 supersedes 声明 + l15.apply_supersession_linkage 落库后联动标记（PR#3，14 测试 + 真实链路验证） |
| T-18 | Task | L1.5 候选池万级预算截断治理（规模化） | ✅ 已解决 | v1.0 | 记忆池破万后候选池 8563 条/76 万字符 > 预算 24000 → 按 priority 截断（2026-08-11 Trae 全量提炼后日志实锤，anomaly_warn 高频）→ 冲突检测质量下降可能漏冲突；→ 2026-08-11 实施：恢复铁律#7——全量召回 + 贪心装箱分批送检，仅单记忆候选超预算 top-k 截断并记 anomaly_warn（PR#2，测试全绿 + 真实链路冒烟） |
| T-19 | Task | Trae 适配器（桥接 + 历史导入） | ST-23 | ✅ 已解决 | — | adapters/trae/（bridge.py 瘦桥接：jsonl 摘要 → L0 转换/append/提炼触发/inject/search，MCP 直连无 hook；import_history.py 幂等全量导入 + 批量提炼）——2026-08-11 全量提炼测试通过，用户验收可用；对比 reasonix 适配器缺 install.py/README/tests（后续按需补） |
| ST-33 | Story | 多源统一检索：agent 自带记忆文件接入 /v1/search | ✅ 已解决 | v1.0 | 2026-08-20 sessions scope（L0 元数据 LIKE + 读盘摘要），B91 | 现状：/v1/search 已统一 memory（L1.5 记忆池）+ wiki（L2 场景）双源（RRF 融合、带溯源，2026-08-11 盘点确认）；L0 原始层为 SGME 自管内容（等同本地会话，经 append 导入即入库，仅未接入 search 内部范围）；**真正外部源只有 agent 自带记忆文件**（如 Hermes MEMORY.md/USER.md、Claude/Codex 记忆文件——2026-08-11 用户裁定：SGME 能关注的是自己管理的内容，agent 记忆文件才是本地的唯一外部来源）。目标：agent 记忆文件接入统一检索（文件索引 + 同一端点召回）；L0 原文**不纳入检索**（设计意图：仅作溯源根，通过 /v1/sessions/{file_id} 按 ID 回溯读取；原文无 FTS/向量索引，不支持语义与向量搜索，2026-08-11 查证确认）；触发词：2026-08-11 README 卖点讨论 |
| T-20 | Task | 模板注入效果检测机制 | ST-5 | ✅ 已解决 | v1.0 | 2026-08-20 注入命中率+引用覆盖率度量（eval 16 用例），B94 | 现状：评测框架只测提炼质量（L1 F1）与检索排序（RRF），4 个场景模板（daily/coding/work/full）的注入效果无检测手段，靠人判断。目标：注入相关性/有用性度量（如注入命中率、后续对话引用率），或人工评估集；触发词：2026-08-11 README 卖点讨论（「按场景注入」卖点需效果证据） |
| T-21 | Task | skill hub HTTP 管理接口（NAS 局域网场景） | ST-11 | ✅ 已解决 | — | 现状：skill hub 仅 /v1/admin/skills/sync（同步端点），技能编辑（put/get/remove_skill）是 Python API 无 HTTP 端点；NAS 部署后局域网用户无法通过接口管理技能。目标：补 HTTP 管理端点（列表/读取/写入/删除 + 鉴权）；触发词：2026-08-11 README 卖点讨论（用户指出 NAS 场景不能读文件） |
| T-22 | Task | wiki MCP 工具（MCP 工具集补 wiki 检索/浏览） | ST-22 | ✅ 已解决 | v1.0 | 现状：MCP 工具集 9 个（append/inject/search/memory_get/refine_trigger/stats/health/config_get/config_update），无 wiki 工具；wiki 仅有 HTTP 端点（/v1/wiki/*，8 个）。目标：MCP 补 wiki 检索（wiki_search/wiki_pages）供通用 agent 使用；触发词：2026-08-11 README 卖点讨论（多协议接入卖点查证）。**已完成（2026-08-13）**：新增 3 个 MCP 工具——wiki_search（检索 wiki_pages 知识文档，wiki_fts BM25 + LIKE 兜底）、wiki_pages（轻量列表，category 过滤）、wiki_page（详情全文，404 语义错误）；新建 operations/wiki.py（search/list_pages/get_page 薄封装，补 wiki 扩展的 operations 层）；测试 test_mcp_wiki.py 7 用例 + 全量 pytest 绿；文档：架构 §22 5.1 工具集 13→16、agent-onboarding 清单更新、变更记录 B40。**数据源边界查证（2026-08-13）**：wiki_pages 知识文档 ≠ L2 scenes 场景——wiki 三工具操作 wiki_pages（对称 /v1/wiki/*）；L2 场景检索仍走 search 工具（scopes=wiki，v0.8 统一待后续） |
| T-23 | Task | 标准安装布局：SGME_HOME 数据外置 + ~/.sgme/install.json 落地 | ST-23 / EP-2 | ✅ 已解决 | v1.0 | 2026-08-20 install.py 生成 install.json（不落明文 key），B93 | 现状：README 快速开始无标准安装目录（clone 就地跑），DATA_DIR/RAW_DIR 硬编码项目根（config.py:168-169，12 模块统一引用常量）；ST-23⑦ install.json 仅设计未落地。2026-08-11 用户定案：Windows 惯例安装——程序 %LOCALAPPDATA%\sgme、数据/配置 ~\.sgme。目标：①config.py 支持 SGME_HOME env 重定向 data/raw/用户配置（未设=现状项目根，零回归）②启动时生成 ~/.sgme/install.json（地址/端口/key 引用，ST-23⑦ 服务发现落地）③README 补「安装」章节（待验证后写）；触发词：2026-08-11 笔记本新用户流程测试（卸载重装验证暴露安装体验缺口）；**已完成（2026-08-11 PR#1/PR#2）**：①SGME_HOME 重定向（config.py + USER_ROOT 基准 5 处 + LOG_DIR，7 测试）②install.json 生成（lifespan 生产模式写入，key 只引用 env 名，3 测试）——全量 pytest 绿 + 真实链路冒烟通过；③README 安装章节待笔记本新用户实测后补写 |
| T-24 | Task | MCP 通道鉴权 + key 反查溯源（产品化前置） | ST-24 | ✅ 已解决 | v1.0 | 现状：MCP 通道（9913）完全无鉴权（_require_admin 零调用点），agent_id 自报不可信。目标：与 HTTP 同规则鉴权（X-API-Key 校验）+ append key→agent_id 反查兜底。**已完成（2026-08-11 PR#1+#2+#3）**：ApiKeyMiddleware（复用 AgentKeyStore.is_agent，缺失/无效 403）+ 手动 uvicorn 跑 streamable_http_app（替代 mcp.run 自托管，后者忽略中间件）+ append ctx 反查（显式 > key 反查 > None）；测试 +8，全量 1505 passed；文档 B37；客户端须带 X-API-Key 连接 |
| T-25 | Task | L1.5 候选池向量预筛（提炼成本治理） | ST-24 | ✅ 已解决 | v1.0 | 现状：维度 OR 全量召回在记忆库 9k+ 时单次 l1_conflict 消耗 67-100 万 tokens（Trae 导入 1.27 亿 tokens 的 98.7% 来自 l1_conflict；全库 88% token 消耗归因单次顶格调用）。目标：候选 = 向量 Top-K ∪ 维度 Top-N（priority 降序），单记忆候选 ≤ vector_top_k + dimension_top_n（默认 50+50），单次降到 ~2 万 tokens（降 98%）。**已完成（2026-08-12 PR#4）**：l15.build_candidate_groups 预筛路径（_build_prescreened_candidates，embed 不可达/向量检索异常自动回退全量召回宁贵勿漏）+ config/sgme.yaml l15.prescreen 段 + 9 测试 + 全量 pytest 绿 + 9911 真实冒烟（embedding 火山 200 OK ×4、l1_conflict 750 tokens、零降级）；期间提炼临时关闭（dream/batch_scan/插件 refine_on_end 三源全关，修复后恢复） |
| T-26 | Task | 创意池 ideas 维度注册 + HTTP 管理端点接线（WebUI ST-7 前置） | ST-14 / ST-7 | ✅ 已解决 | v1.0 | 现状：创意池（ST-14 标已解决）存在**两个未落地缺口**——①``ideas`` 维度从未注册（registry/dimensions.yaml 无此 id），创意池定义「带 ideas 维度 + ttl_days=NULL 的记忆」无数据源，L1 提炼无法打标签、memory_tags 因 FK 无法写 ideas；②``/v1/admin/ideas`` HTTP 路由未注册（operations/idea.py 8 操作 + data/idea_dao.py 已就绪，app.py 未 include，ST-17 创意池管理 UI 无后端）。**已完成（2026-08-12）**：①registry/dimensions.yaml 注册 ideas 维度（动态、ttl_days=null 长期保存）+ aliases.yaml 加别名（创意/想法/灵感/点子/思维闪光/灵感火花）；②新建 sgme/server/routes_ideas.py（7 端点：list/get/patch/notes/flag/delete/restore，纯协议翻译层）+ app.py include_router、修复 dimension_id 默认值覆盖 bug（路由显式传 None 会把 operations 层「默认过滤 ideas」变成「不过滤维度」）；③tests/test_routes_ideas.py 10 用例全绿 + 全量 pytest 无回归 |
| T-27 | Task | 创意池升格联动端点 `POST /v1/admin/ideas/{id}/promote`（ST-7 闭环） | ST-7 | ✅ 已解决 | v1.0 | 现状：创意池「升格」缺联动——ST-14 文档注明「跨模块耦合留到集成阶段」，仅置 custom_flag 不创建需求，溯源链（demands.origin_idea_id）无法闭合。设计（SGME-WebUI设计-v0.1 §5）：升格 = 置 promoted 标记 + 创建 demand 回填 origin_idea_id。**已完成（2026-08-12）**：operations/idea.py `promote_idea`（置 flag 先置失败即中止 + 函数内导入复用 demand.create_demand）+ routes_ideas.py 新增 promote 端点（title 必填，其余可选）+ tests/test_routes_ideas.py 增 2 用例（升格闭环 / title 400 + 不存在 404）；设计文档已更新标记实现 |
| T-28 | Task | WebUI 前端骨架 + FastAPI 静态托管（ST-7 前置） | ST-7 | ✅ 已解决 | v1.0 | 设计定案（2026-08-12）：前后端分离 SPA（Vue3+Vite）+ FastAPI 托管静态（SGME-WebUI设计-v0.1 §1）。**已完成（2026-08-12）**：①`ui/` 骨架（package.json/vite.config.ts dev proxy 9910/tsconfig/src：router 4 导航 + api/client（Admin Key 管理 + ERR_* 错误封装）+ api/ideas + MainLayout 侧边栏 + 创意池完整页 IdeaList（列表/编辑/备注/标记/升格）+ 占位页）；`npm run build` 通过（38 模块）；②app.py 挂载 `ui/dist`（存在即挂载）+ SPA catch-all（非 /v1 回 index.html，排除 v1/docs/openapi）；③.gitignore 排除 `/ui/dist/` `/ui/node_modules/`；④测试适配 2 处（test_routes_templates 路径穿越 405、test_routes_ideas 鉴权 4xx）；全量 pytest 绿 |
| T-29 | Task | WebUI ③ 创意与需求导航页面（创意池/创意详情/需求池/项目注册表） | ST-7 | ✅ 已解决 | v1.0 | 按 SGME-WebUI设计-v0.1 §4 视图 12-16 实现。**已完成（2026-08-12）**：api client 封装（demands.ts/projects.ts）+ 4 页面——IdeaList（列表/检索/编辑/备注/标记/升格）、IdeaDetail（详情/溯源/备注追加/升格联动，URL /ideas/:id）、DemandList（列表/状态过滤/状态流转 pending→planned→partial→done）、ProjectList（列表/编辑 milestone/git/path）；MainLayout 侧边栏展开 ③ 子导航 + router-link-active 激活态修正；`npm run build` 通过（49 模块）；①②④ 导航占位页已由 T-30~T-32 补齐（2026-08-12/13） |
| T-30 | Task | WebUI ① 总览 Dashboard 页面（健康/概览/提炼监控/Dream 日报/事件流） | ST-7 | ✅ 已解决 | v1.0 | 按 SGME-WebUI设计-v0.1 §4 视图 1-5 实现（2026-08-12/13）：ui/src/views/dashboard/DashboardView.vue（389 行）+ api/dashboard.ts——消费 /v1/health、/v1/admin/stats、refine_runs（stage/status 分页）、Dream 日报（列表+手动触发）、/v1/events 事件流；健康卡片 + 计数卡片 + 提炼记录 + 日报 + 事件流多区块单页；路由 /dashboard |
| T-31 | Task | WebUI ② 记忆与知识导航页面（记忆列表/详情/场景/统一检索/会话原文/Wiki） | ST-7 | ✅ 已解决 | v1.0 | 按 SGME-WebUI设计-v0.1 §4 视图 6-11 实现（2026-08-12/13）：MemoryList（373 行，维度/状态/排序/时间窗过滤 + ttl_filter）、MemoryDetail（溯源/拒绝/恢复）、SceneList（状态标记 active/rejected/expired）、SearchView（/v1/search 统一检索 + 溯源）、SessionView（L0 原文按 file_id 查看）、WikiView（列表/详情/导出）；api client：memory.ts/knowledge.ts/wiki.ts |
| T-32 | Task | WebUI ④ 配置与管理导航页面（设置 9 标签页 + 技能仓库） | ST-7 | ✅ 已解决 | v1.0 | 按 SGME-WebUI设计-v0.1 §4 视图 17-22 实现（2026-08-12/13）：SettingsView 单入口 + 9 标签页（通用设置/模型供应商与降级链/TTL 配置/模板管理/Agent 管理/维度注册表/提示词/扩展模块/备份管理——含 templates/registry/agents/prompts/config/backup 等 redirect 合流）；SkillsView（382 行，技能仓库独立导航页，/v1/admin/skills 消费）；api client：admin.ts/llm.ts/skills.ts |
| T-33 | Task | LLM 供应商与降级链管理端点（WebUI 设置页后端） | ST-7 | ✅ 已解决 | v1.0 | 现状：LLM 配置只有文件（llm.yaml 链 + providers.yaml 连接表），无管理端点；WebUI 供应商页无后端。**已完成（2026-08-12/13）**：sgme/operations/llm.py + sgme/server/routes_llm.py——GET /v1/admin/llm（降级链结构+链级规则+供应商连接信息）、GET /v1/admin/llm/health（逐供应商健康探测）、POST /v1/admin/llm/providers（新增/更新连接表）、DELETE /v1/admin/llm/providers/{provider}（被链引用时拒绝）；app.py include_router；tests/test_routes_llm.py + tests/test_routes_skills.py |
| T-34 | Task | 统一搜索新增 wiki_pages scope（wiki 知识库页面纳入 /v1/search） | ST-7 | ✅ 已解决 | v1.0 | 现状：/v1/search 只有 memory + wiki（L2 scenes）两层，wiki.db 知识页面（wiki_pages）走独立通道（/v1/wiki/search + MCP wiki_search），WebUI 统一检索不含知识库——用户判定功能不完善（2026-08-13）。**已完成（2026-08-13）**：operations/search.py 新增 wiki_pages scope（search() 加 wiki_conn 参数，复用 sgme.wiki.fts.search_wiki_fts 不造轮子，**容错隔离**——wiki_conn 为 None 或检索失败该层空结果 + WARNING，不影响 memory/scenes 层）；routes_memory.py /v1/search 端点经 app.state.wiki_conn 注入；结果形状 source=wiki_pages、routes=[wiki_fts]、含 page_id/title；tests/test_operations_search.py +5 用例（命中 / 连接为 None 跳过 / 检索失败隔离 / memory+wiki+wiki_pages 三层组合 / HTTP 端点注入）；test_fast search 55 passed；文档：架构 §检索 scope 枚举 + 变更记录 B42；MCP search 保持 memory-only（wiki_pages 已由 wiki_search 工具覆盖）。**WebUI 同步（2026-08-13）**：SearchView 增加 wiki_pages scope 勾选（默认开启）+「知识库」标签 + 结果展示 title + 点击跳转 /wiki?page_id= 直达 Wiki 详情（WikiView 消费 query 自动打开）；api/memory.ts SearchResult 补 page_id/title |
| T-35 | Task | Care Engine 角色层数据结构（Character Card V2 兼容子集 + 角色 persona 物化生成） | ST-25 | ✅ 已解决 | — | 按 SGME-CareEngine设计-v0.1 §角色层。**已完成（2026-08-13）**：①sgme/care/roles.py——CC V2 兼容子集（spec/data 双层，顶层/data/extensions.sgme_care 白名单校验，必填 name/description，角色 id 防路径穿越）+ 文件 CRUD（save/list/get/archive 移 .archive 原件永不删）+ persona 物化（Persona Architect 四层扫描提示词 ≤2000 字、落盘 data/personas/、备份轮转保留 3 份）②operations/care.py——list/get/create/delete/persona 生成（画像素材=记忆池静态维度 identity/preferences/habits/values/style/skills/family/social 聚合零物化；复用 sgme.llm.chain.call_with_fallback 降级链；LLM 不可用 ERR_INTERNAL 不降级直存）③routes_care.py 6 端点（GET/POST/DELETE /v1/admin/roles[/{id}] + persona GET/POST）④config：care 段（enabled/persona_max_chars）+ DEFAULT_CARE_CONFIG 兜底合并 + ROLES_DIR/PERSONA_DIR ⑤roles/butler.json 预置管家角色（含 sgme_care 关怀策略：问候模板/触发规则含透析日提醒/频率档位）⑥tests/test_care.py 28 用例全绿；test_fast care 28 passed；文档：设计文档 §Task 状态 + 变更记录 B43 |
| T-36 | Task | Care Engine 关怀信号增强（信号类型枚举 + 事件标记规则 + 与 Dream 协同） | ST-25 | ✅ 已解决 | — | 按设计 v0.1 §关怀信号。**已完成（2026-08-13）**：sgme/care/signals.py——四类关怀信号（care_todo_due 待办到期：tasks 维度 updated_at 老化 ≥7 天；care_mood 情绪：status 维度关键词匹配可配置；care_overwork 过劳：focus 维度当日新增 ≥5；care_daily 每日关怀），**零 LLM 规则引擎**（SGME 只发信号不做决策）；幂等去重=uuid5(type+dedup_key) 确定性事件 id + INSERT OR IGNORE（重复扫描零重复）；operations/care.py +3 操作（scan/list/consume）；routes_care.py +3 端点（POST /v1/admin/care/scan、GET /v1/admin/care/signals?unconsumed_only、POST .../consume）；与 Dream 协同=消费方定时调用 scan（常驻轮询不做）；tests +6（四类推导/幂等/关键词配置/列表消费/HTTP 全链路）；test_fast care 24 passed；文档：变更记录 B44；目标矛盾推导（goals/values 交叉）规则待实施期收敛 |
| T-37 | Task | Care Engine 角色注入装配（角色 persona + 画像模板查询 → 沟通提示词；角色读写记忆闭环） | ST-25 | ✅ 已解决 | — | 按设计 v0.1 §角色层。**已完成（2026-08-13）**：operations/care.py `assemble()`——角色卡 system_prompt（{{original}} 占位替换为角色职责默认文案，{{char}}/{{user}} 宏保留给消费方）+ persona 物化全文（若已生成）+ profile_blocks 用户画像（inject 模板查询零物化，可选 inject_mode=daily/full）+ care_policy 关怀策略（greeting_templates/trigger_rules/frequency）；**换皮不换芯**——换角色 = 换装配输出（表达风格+主动策略），记忆池不动；GET /v1/admin/roles/{id}/assemble?inject_mode= 端点；tests +5（无 persona/有 persona/带画像/角色不存在 404/HTTP）；test_fast care 175 passed（care + care_consumer） |
| T-38 | Task | Care Engine 关怀消费方（Hermes 插件：cron + heartbeat 双轨消费信号） | ST-25 | ✅ 已解决 | — | 按设计 v0.1 §消费方。**已完成（2026-08-13）**：scripts/care_consumer.py（项目内随 git，非 Hermes 插件部署副本）——①触发 SGME 关怀扫描（POST /v1/admin/care/scan 幂等）②拉取未消费信号（?unconsumed_only=true）③**幂等去重**（本地状态 data/care/consumer_state.json，last_notified_at 模式防 consume 失败重复）④stdout JSON 行输出待关怀事项（空=静默）⑤标记已消费；--check-only 供 heartbeat 巡检（只查不消费）；SGME 不可达静默降级（不阻塞宿主）；key 从 config/.env 读不落盘；cron 定时（完整流程）+ heartbeat（--check-only）双轨由 Hermes 平台 cron 调度（T-38 交付脚本 + 注册方式）；tests/test_care_consumer.py 5 用例（无信号静默/输出+消费+幂等/check-only 不消费/不可达降级/缺 key 跳过）；test_fast care 175 passed |
| T-39 | Task | WebUI 角色管理页（角色列表/详情/编辑/新建/归档 + persona 生成查看） | ST-7 | ✅ 已解决 | — | 角色系统（ST-25/T-35）已交付 /v1/admin/roles* 6 端点，WebUI 需要管理界面：角色列表（卡片式展示 name/description/关怀策略摘要）、角色详情（CC V2 字段可读）、编辑/新建（表单校验 name/description 必填）、归档（移 .archive）、persona 生成/查看（POST/GET persona 端点，生成时展示 LLM 产物）；路由建议 /roles 挂记忆与知识导航（②区）；触发词：2026-08-13 用户要求——为角色模板生成内置模板给用户选择，WebUI 需角色管理入口；前端 ui/src/views/ 新建 + api client（roles.ts）。**已完成（2026-08-13）**：RolesView.vue（master-detail：角色卡片列表含关怀策略摘要/详情含触发规则与频率/persona 生成与全文/内联编辑/新建/归档）+ api/roles.ts 6 端点封装 + 路由 /roles + 侧边栏 🎭；npm run build 通过 |
| T-40 | Task | WebUI 当前角色选择与装配预览（会话级角色切换 + assemble 预览） | ST-7 | ✅ 已解决 | — | 用户选择当前沟通角色（内置 4 个：管家/伴侣/朋友/导师，见 roles/），选择持久化（config 或独立文件，实施期定），切换即换皮不换芯（记忆池不动）；装配预览：GET /v1/admin/roles/{id}/assemble?inject_mode= 结果展示（system_prompt + persona + 画像块 + 关怀策略），供用户理解"这个角色会怎么跟我说话"；与 Hermes 插件装配接线（消费方读取当前角色）留集成期；触发词：2026-08-13 用户要求内置角色模板供选择。**已完成（2026-08-13）**：SGME 侧 GET/PUT /v1/admin/care/active-role（运行数据 data/care/active_role.json，角色不存在 404）+ 前端当前角色徽标/设为当前按钮/装配预览（system_prompt+画像块数+persona+关怀策略）；真实链路验证：PUT butler → 读取一致，assemble 注入 4 个真实画像块 |
| T-41 | Task | WebUI 关怀信号面板（scan 触发 + 未消费信号列表 + 消费） | ST-7 | ✅ 已解决 | — | 关怀信号（T-36）端点已交付：POST /v1/admin/care/scan、GET /v1/admin/care/signals、POST .../consume；WebUI 面板：手动触发扫描按钮、未消费信号列表（类型徽标 care_todo_due/care_mood/care_overwork/care_daily + payload 内容 + 时间）、消费按钮、历史查询；让用户能看到"管家在关心什么"；路由建议 /care 或并入设置页扩展模块标签；触发词：2026-08-13 ST-25 收尾后 WebUI 完整性。**已完成（2026-08-13）**：SignalsView.vue（触发扫描幂等统计/类型徽标/未消费过滤/单条消费/payload 摘要）+ api 封装 + 路由 /signals + 侧边栏 💗；build 通过 |
| T-42 | Task | L2 场景消费端接线（对话内容驱动的场景语义匹配注入） | ST-5 | ✅ 已解决 | — | 背景：用户质疑 L2 没起作用（2026-08-13）——L2 抄自腾讯 L2 Scenario（快速恢复工作上下文），SGME 移植后只在搜索支路、注入不消费（结构性缺位）；用户决策：先不砍，接上消费端（方案 1）。**最终实现（2026-08-13，先错后正）**：第一版 inject 场景速览（会话打开固定注入）被用户纠正——场景注入应由 agent 根据对话内容语义匹配（「和场景注入关联才对」），已 revert（124429f）；改 Hermes 插件 prefetch scopes=[memory, wiki]（8b6d422）——每轮 LLM 前用当前对话检索场景（FTS BM25+向量+RRF 相关性召回），分块渲染「# 相关场景（L2 匹配）」，场景未命中不占 token；SGME 侧零改动；真实链路：SGME/Trae/记忆引擎 三 query 各命中对应场景；插件已部署（重启 Hermes 生效）；ChronoMemo 时光轴数据源待立项接线；文档：变更记录 B47 |
| T-43 | Task | 提炼 LLM 动态链（未指定跟随 agent 当前 LLM；指定后 agent 降备用；向量模型进供应商设置） | ST-5 | ✅ 已解决 | — | 功能变更（2026-08-13 用户定）：①用户未指定专用提炼 LLM 前，首选 = agent 当前 LLM（直接复制其模型调用参数）；②用户可调整并指定专用提炼 LLM，然后由当前使用的 LLM 作为备用；③不建议引导本地模型（向量维度/能力不够），但允许用户自定义提供商（不建议≠不可以）；④供应商设置补向量模型指定（原无 embedding 配置位）。**已完成（2026-08-13，commit 2426361）**：**注册声明制**（可行性修正：Hermes MemoryProvider.sync_turn 无 model 参数，无法每轮自动跟随——改为 agent 注册/append 声明 agent_model=provider/model，落 raw_files）；sgme/llm/resolve.py 动态链构造（refine.llm_override 空=跟随 agent 复制 providers 连接参数；指定=专用为主 agent 为备；provider 缺失跳过、未声明回退静态链零破坏）；append 请求体加 agent_model（未传按 agent_id 反查注册声明）；providers.yaml 新增顶层 embedding 段（向量模型指定入口，search.vector 缺连接字段时兜底注入）；真实链路：注册声明→append 反查落库→动态链 [deepseek, lm-studio]；tests +8（跟随/override 优先/未知 provider 跳过/未声明回退/格式非法/override 缺失/纯函数）；文档：变更记录 B48。**WebUI 向量模型配置位（2026-08-13 同步）**：后端 llm_status 暴露 embedding 段 + vector_current；新增 PUT /v1/admin/llm/embedding/active（切换写回 search.vector 并 persist_config，provider 不存在 404）；config 增 load_embeddings_config 公开读取；tests +7（llm_status 暴露/文件读取/切换落盘/未知 404/路由+鉴权）；前端 ProvidersView 增加「向量模型」区块（复用供应商卡片形态，展示 display_name/默认模型/可用模型/base_url/密钥 + 「使用中/设为当前」，当前 provider 高亮），api/llm.ts 补 embedding/vector_current + setActiveEmbedding |
| T-44 | Bug | WebUI 模型供应商页空白（前端渲染崩溃） | ST-7 | ✅ 已解决 | — | 根因：新增向量模型区块后，前端 `Object.values(status.embedding)` 在后端旧进程未返回 `embedding` 字段时对 undefined 抛 TypeError → 组件崩溃整页空白。修复（2026-08-13）：ProvidersView 对该字段加 `|| {}` 容错（`Object.values(status.embedding || {})`），dist 重建即可恢复（FastAPI 静态托管实时读磁盘，无需重启后端）。**经验：前端消费后端新增字段必须容错旧后端缺失字段** |
| T-45 | Task | WebUI 首屏自动填充 Admin/Agent Key | ST-7 | ✅ 已解决 | — | 用户诉求：key 存于服务器 config/.env（浏览器看不到），每次手动输入麻烦。实现（2026-08-13）：后端新增 `GET /v1/admin/keys`（**仅限本机回环来源**，不要求 key 鉴权——前端首次无 key 无法鉴权，故以来源校验代替；key 本就在本机 .env，本机回环可信，远程 403 防泄漏）；前端 client.ts `autoFillKeys()`（已存 key 则跳过，避免覆盖手动配置）+ MainLayout onMounted 调用并刷新侧栏 key 显示；tests +3（本机返回 / 自定义 key / 远程 403）；**需重启后端加载新端点** |
| T-46 | Task | WebUI 统一检索 wiki_pages scope 前端同步 | ST-7 | ✅ 已解决 | — | 见 T-34 备注「WebUI 同步」段：SearchView 加 wiki_pages scope（默认开启）+ 知识库标签 + title 展示 + 点击跳 /wiki?page_id= 直达详情；WikiView 消费 query 自动打开；api/memory.ts 补 page_id/title |
| T-47 | Task | 统一供应商模型 + 降级链可编辑（WebUI 供应商页升级） | ST-7 | ✅ 已解决 | — | **T-43④ 深化**（2026-08-13 用户定）：向量提供商从 providers.yaml 顶层 embedding 段并入顶层 providers 段，统一以 `vector_capable=true` 标记；embedding 段仅向后兼容保留，**不再是主入口**。**后端**：①config.py `write_providers_config` 修正——写回只覆盖 providers 段、保留 embedding 等其余段（否则供应商管理操作抹掉向量配置）；新增 `write_llm_config`（只覆盖 chains 段、保留 rules 段，供降级链编辑）②operations/llm.py——`llm_status` 返回统一 providers（各供应商带 vector_capable/models 标记）、`llm_provider_add` 支持 vector_capable 字段、`llm_embedding_set_active` 改为从 vector_capable 供应商选向量模型（兼容旧 embedding 段）、新增 `llm_chain_update`（整体更新降级链：校验供应商存在 + 白名单黑名单 + 写回 llm.yaml 刷新运行时）③routes_llm.py 新增 `PUT /v1/admin/llm/chains`。**修复真实 bug**：llm_chain_update 调 validate_models 时未带 rules 导致黑名单校验失效（已修）。**前端**：llm.ts 补 vector_capable/models/display_name + updateChains API；ProvidersView——降级链+链级规则并排两栏、降级链可编辑（增删/上下排序/选供应商/填模型 + 保存/撤销）、供应商表单加「向量模型」checkbox、供应商卡片显示「向量」标签、向量模型区块改为从 vector_capable 供应商中选。测试：test_routes_llm +13（落盘保留 embedding/vector_capable 标记/链更新落盘+保留 rules/未知供应商拒绝/黑名单拒绝/校验失败不污染文件/路由+鉴权）；test_fast llm 85 passed；npm run build 通过。文档：变更记录 B49。**布局与探测收尾（2026-08-13 用户反馈）**：①「供应商」改名「模型供应商」并移至最上、「降级链」改名「模型降级链」、删除「链级规则」卡片（降级链不再并排两栏）②保存供应商（含向量标签）后顶部横幅反馈（原表单关闭后无提示）③llm_health 探测范围并入 vector_capable 供应商（向量模型连通性也随「刷新健康」探测），向量模型卡片显示连通状态；test_routes_llm +1（vector_capable 纳入健康探测）；test_fast llm 86 passed；build 通过 |
| T-48 | Task | 三池职责重构：创意用户驱动 + 需求池改跨项目待办 + 项目主动立项 | ST-14 / ST-15 / ST-16 | ✅ 已解决 | v1.0 | **2026-08-13 用户定**：①**创意池不再由 LLM 自动识别**——prompts/l1_extraction.txt 移除创意识别指令（第 5 条删、第 3 条加否定式"ideas 禁止自动标注"），创意只由用户主动提出才记录；②**需求池改为跨项目待办池**——demands 语义变更：project_id 从 400 硬校验降级为 warning 不阻断（待办可先于项目注册出现），状态机 pending→done + created_at/resolved_at 时间戳，agent 维护；③**项目池用户主动立项**——WebUI/对话命令登记 project_meta；④MCP 新增 idea_add/demand_create/project_register 三工具（ONBOARDING_TOOLS 同步，agent 对话内即可"记个创意/建待办/立项"）；⑤WebUI 三池完整 CRUD——创意页新建弹层、待办页新建/编辑+项目过滤+时间展示、项目页立项弹层，导航"需求池"→"待办"；⑥project_init.py 第⑤步接线待办池关联（检索标题含项目名的待办→标 planned+绑 project_id）；⑦存量 ideas 维度 ttl=90 回填 NULL（2 条，备份 data/memory.db.bak-ttl-backfill-20260814-003644）。文档：AGENTS.md 新增「三池职责」段、SOUL.md 立项铁律镜像同步、变更记录 B50 |
| ST-26 | Story | DeepSeek Harness 原生插件适配（多 Agent 共享记忆接入 dsh） | ✅ 已解决 | v1.0 | dsh 原生 TS 插件 + Python install 引导，复用 SGME HTTP API（inject/search/append/refine），运行时零 Python 依赖；AC：①TS 插件遵循 dsh Cordis 规范（apply/Config/inject/export name）②5 类能力：画像首步注入（agent/pre-step + agent.inject）、memory_search 工具、wiki_search 工具（差异化）、/sgme 命令、turn/end 会话入库 ③install.py 注册 agent+写 .env+打印 dsh plugin add 命令 ④pytest + vitest 全绿 ⑤真实 LLM 冒烟无回归 ⑥dsh 本地加载验证；触发词：2026-08-14 DeepSeek Harness 开源（github.com/deepseek-ai/deepseek-harness），SGME 多 Agent 定位需要专用适配器接入 dsh 生态；技术决策：v1 首步注入+每 turn append，每轮注入/攒批留 v2；插件代码随 git 管理（adapters/dsh/sgme-bridge/），构建产物 lib/ 提交（dsh-mnemon 模式）。**已完成（2026-08-14）**：T-49~T-54 全绿，dsh 本地加载验证通过（memory_search 工具+ /sgme 命令+ turn/end 累积式同步入库），2 条 dsh 会话已入库 SGME session.db 并自动提炼；session-sync v1.1 修复了 turn/end 不含 messages 的关键问题（累积 user/assistant/tool 事件） |
| T-49 | Task | dsh 适配器 Python 侧骨架（adapters/dsh/ + install.py + import_history.py） | ST-26 | ✅ 已解决 | — | install.py（注册 agent+写 .env+打印 dsh plugin add 命令，对齐 reasonix install.py）+ import_history.py（历史会话补导入，与 reasonix 同款幂等可重跑）+ README + .gitignore + pytest 测试；不含运行时逻辑（运行时全在 TS 插件内）。**已完成（2026-08-14）**：adapters/dsh/ 骨架完成，install.py 注册 agent_id=dsh + 写 .env + 打印 dsh plugin add 命令，import_history.py 幂等导入历史会话 |
| T-50 | Task | sgme-bridge TS 插件骨架（package.json + cordis.patch.yml + tsdown + vitest 配置） | ST-26 | ✅ 已解决 | — | 标准结构：package.json（@sgme/dsh-bridge + dsh.bundle 字段）、cordis.patch.yml、tsdown.config.ts、vitest.config.ts、tsconfig.json、src/ 目录占位、lib/ 产物；pnpm verify 通过。**已完成（2026-08-14）**：骨架完整，tsdown 构建产物 lib/index.js 23.51 KB（gzip 8.06 KB），pnpm typecheck 通过 |
| T-51 | Task | sgme-bridge TS 插件核心能力（sgme-client + 5 类能力） | ST-26 | ✅ 已解决 | — | src/index.ts（apply+Config schema）+ sgme-client.ts（fetch HTTP 客户端，trust_env=False 等价防代理劫持）+ tools.ts（memory_search/wiki_search defineTool）+ context.ts（agent/pre-step 首步画像注入+agent.inject）+ commands.ts（/sgme 命令）+ session-sync.ts（session/event 拦 turn/end → /v1/append + /v1/admin/refine/trigger_async）；故障隔离（失败只 log 不阻塞）；密钥从 Config 注入不硬编码。**已完成（2026-08-14）**：5 类能力全部实现并修复 dsh 规范兼容性问题（命令单参数对象、工具 defineTool + output schema、事件监听 session/event 而非 agent/pre-step）；session-sync v1.1 改造为累积式（见 T-53） |
| T-52 | Task | dsh 适配器测试（pytest install/import_history + vitest sgme-client/tools/context） | ST-26 | ✅ 已解决 | — | Python 侧 pytest test_install.py + test_import_history.py（对齐 reasonix 同款）；TS 侧 vitest sgme-client.test.ts + tools.test.ts + context.test.ts（mock fetch/ctx）；全量 pytest 无回归。**已完成（2026-08-14）**：vitest 65 用例全绿（session-sync 12 + sgme-client 19 + tools 12 + skeleton 5 + 其他），pytest 17 用例全绿 |
| T-55 | Task | 免费模型托底产品化（默认配置模板 + Key 缺失引导提醒 + 向量健康检查） | ST-6 / ST-4 | ✅ 已解决 | v1.0.0b3 | 背景：免费模型托底调研（2026-08-17 wiki research 页）→ 2026-08-18 用户定：①普通用户默认免费双件套——LLM 提炼智谱 GLM-4.7-Flash（永久免费、注册零充值，官方「免费模型」分类）+ 向量硅基流动 BAAI/bge-m3（1024 维免费，实名解锁；官方口径：免费模型费用 0、向量 RPM 2000-10000）②本机向量也切硅基 1024（放弃 2048 兼容，接受全量重灌 backfill --force 11,568 记忆 + 109 场景，免费额度内约 1-2 小时）③智谱 embedding-3（2048 维、0.5 元/百万）保留为可选不纳入默认路径；✅ 已产出：申请指南 docs/guide/免费模型Key申请指南.md + wiki skill/sgme 页（2026-08-18）；待办：a) providers.yaml 加 zhipu 条目 b) llm.yaml 提炼链 deepseek 后插 glm-4.7-flash 兜底节点 c) sgme.yaml search.vector 切 siliconflow/BAAI/bge-m3 d) Key 缺失检测引导（health model_config / inject stats.note / agent_onboarding 提醒，附申请地址）e) 本机全量重灌 f) 测试 + 变更记录 B 系列 + README 引导；**进度（2026-08-18）**：a✅ b✅（链 [zhipu主→deepseek备→rule]）c✅ d✅；**新增（用户定）**：向量健康检查 + provider 注册 zhipu（修复未知 provider 崩溃）；测试 86 passed；真实冒烟通过；变更记录 B79-B84；**收尾（2026-08-18 b3 发布）**：e✅ 重灌完成验证（NAS 实测 vector available、记忆 10638+场景 230、search 命中 vector 路由）f✅ README/agent 文档同步——架构 v0.9 §24 降级链+§23 向量模型、接口契约「两个模型」表、runbook §16.3 全部对齐为 zhipu 主链/siliconflow bge-m3；版本号 1.0.0b2→1.0.0b3（B85），已提交推送+tag+GitHub release |

| T-53 | Task | dsh 适配器真实链路验证（SGME 冒烟 + dsh 本地加载） | ST-26 | ✅ 已解决 | — | ①python scripts/e2e_smoke_v04.py 真实 LLM 冒烟无回归 + 查日志无 L1.5 解析失败/降级直存 ②dsh plugin --profile web add "link:D:/Projects/SGME/adapters/dsh/sgme-bridge" ③dsh --profile web --dump-config 确认挂载 ④/sgme 测试 验证检索 ⑤发几轮对话查 /v1/admin/sessions 确认 L0 入库。**已完成（2026-08-14）**：①dsh plugin --profile web add 成功挂载 ②dsh --profile headless "say hi" 成功响应 ③memory_search 工具调用返回真实 SGME 记忆 ④/sgme 命令执行返回详细记忆汇总 ⑤session-sync v1.1 修复 turn/end 不含 messages 的关键问题（解压 session.jsonl.zstd 确认事件结构，累积 user/message + assistant/message + tool/result 事件到 turn buffer，turn/end 触发 /v1/append）⑥2 条 dsh 会话已入库 SGME session.db（agent_id=dsh，status=refined 自动提炼完成）⑦L0 文件格式正确（YAML frontmatter + # user / ## assistant 消息块）；验证用 reasonix agent key（memory:rw）+ admin key（triggerRefine） |
| T-54 | Task | dsh 适配器文档（README + Backlog 状态 + B53 变更记录） | ST-26 | ✅ 已解决 | — | README 接入说明（前置条件/安装/验证/排障）+ Backlog ST-26/T-49~T-53 标 ✅ + B53 变更记录（背景/改动/测试/运维影响）。**已完成（2026-08-14）**：Backlog ST-26/T-49~T-54 全部标 ✅；B53 变更记录已写入 docs/design/SGME-实施变更记录-v0.9.md；README 接入说明见 adapters/dsh/sgme-bridge/README.md |
| T-55 | Task | wiki 直接写入正式 API（HTTP + MCP，不走提炼通道） | ST-22 | ✅ 已解决 | — | 触发词：2026-08-14 讨论「SGME wiki 成为渐进式 skills hub」（分析报告已入 wiki：sgme-wiki成为渐进式skills-hub的可行性分析报告-57e75e55）。**已完成（2026-08-14）**：①operations/wiki.py 新增 create_page（page_id 复用 _gen_page_id 与提炼链路一致；幂等 upsert 返回 created/updated；**先 init_wiki_fts 再 insert_page 保证 FTS 立即可搜**——冷启动库也成立）②HTTP POST /v1/wiki/pages（require_agent_key；Pydantic 必填缺失 422、业务校验空串 400）③MCP wiki_page_add（ONBOARDING_TOOLS 17→18；缺必填参数框架层 ToolError 与 wiki_page 一致）④测试 test_mcp_wiki.py +4、test_wiki.py +4（28/28 绿）⑤脚本 scripts/wiki_add_page.py 沉淀（内部运维通道保留）⑥文档：架构 §10.2 端点清单、变更记录 B10 |
| T-56 | Task | 创意池独立建表（ideas 维度独立日：memories 打标 → ideas 表） | ST-22 | ✅ 已解决 | — | 触发词：2026-08-14 用户拍板「维度独立日」——创意/项目/待办均由接入 agent 掌控，LLM 不写创意；goals/tech_stack 独立计划收回（仅 ideas 落地）。**已完成（2026-08-14）**：①IDEAS_DDL + _migrate_ideas_table（memory.db；无 content_seg/FTS——创意不进 /v1/search，ideas API 专属）②idea_dao.py 重写（memories 约束读写 → ideas 表 CRUD，函数接口保持）③operations/idea.py 参数 memory_id→idea_id、去 dimension_id；promote_idea 回填 origin_idea_id=idea_id ④routes_ideas.py URL {memory_id}→{idea_id}（契约变更，消费方仅 WebUI+agent）⑤MCP idea_add 契约不变（docstring 更新）⑥WebUI ideas.ts Idea 接口换 idea_id + 去 memories 字段；IdeaList/IdeaDetail 同步 ⑦scripts/migrate_ideas.py 迁移 23 条（22 active+1 rejected；idea_id=原 memory_id 溯源零破坏；source_ref 23/23 带全；备份 memory.db.bak-ideas-migrate-20260814-001038；幂等重跑 0 写入；memories 原件保留）⑧测试 test_routes_ideas 重写 16 用例（含表化隔离用例）+ test_mcp_idea_add 契约更新（64 passed）⑨真实冒烟 21/21：全端点+升格闭环+MCP+迁移数据可见 ⑩文档：架构 §数据模型 ideas 表 + 三池章节 + B55 |
| ST-27 | Story | 信号消费端闭环（agent 成为消费者 + 三层消费模型 + TTL 归档） | ✅ 已解决 | v1.1 | 背景：ST-25 AC①「信号总线无消费者」未闭环——memory_updated/anomaly_warn/batch_scan_error 三类信号发布后无消费方（2246 条堆积）；care_* 由 care_consumer 固定脚本消费，但用户定（2026-08-14）：关怀信号应由「当前对话 agent」处理，非后台脚本。AC：①三层消费模型（广播 pull → 认领原子抢 → 回执 signal_acks）②signal_events 加 consumed_by + signal_acks 表 ③TTL 归档（异常类 30d / memory_updated 7d / care_* 消费后 7d）④care_consumer 降级默认只读 + --consume 兜底 ⑤dsh 插件 + MCP 同步加信号消费工具（pull/claim/ack）⑥接入纪律补「会话开始消费信号」铁律；触发词：2026-08-14 用户「信号消费=主动关怀，启动开发」。**已完成（2026-08-14，T-57~T-62）**：内核三层消费模型 + 原子认领 + signal_acks 回执表 + TTL 归档；care_consumer 降级；dsh 插件 + MCP 三工具；AGENTS.md/README/架构 §18 同步；全量 pytest 1644 绿 + vitest 65 绿；变更记录 B57。真实链路验证（consume/ack 端点 + MCP）待服务重启后执行 |
| T-57 | Task | 内核：原子认领 + consumed_by + signal_acks 回执表 + TTL 归档 | ST-27 | ✅ 已解决 | — | db.py signal_events 加 consumed_by 列 + signal_acks 表 + 迁移版本；signal_dao.mark_consumed 改原子（UPDATE WHERE consumed_at IS NULL 返回 rowcount）+ 新增 ack_signal/purge_expired；signal/engine 加 claim；care/signals.consume_signal 传 agent_id + 原子认领 |
| T-58 | Task | care_consumer 降级：默认 --check-only + --consume 兜底 | ST-27 | ✅ 已解决 | — | 默认只读（scan+输出不 consume）；--consume 显式兜底消费；consume 走原子认领 + 回执 |
| T-59 | Task | dsh 插件信号消费能力（HTTP） | ST-27 | ✅ 已解决 | — | sgme-client.ts 加 pullEvents/claimSignal/ackSignal；tools.ts 加 signal_pull/signal_claim/signal_ack 工具 |
| T-60 | Task | MCP 信号消费工具 | ST-27 | ✅ 已解决 | — | mcp_server.py 加 signal_pull/signal_claim/signal_ack（ONBOARDING_TOOLS 同步） |
| T-61 | Task | 文档：接入纪律补信号消费 + 架构 §18 更新 | ST-27 | ✅ 已解决 | — | AGENTS.md 第 4 条铁律 + README 中英 + 架构 §18 三层消费模型 |
| T-62 | Task | 历史清理 + 真实链路验证 + 变更记录 | ST-27 | ✅ 已解决 | — | 游标快进（3 订阅者推最新）+ TTL 归档清理 849 条超期 memory_updated（tmp/signal_cleanup.py）；变更记录 B57；真实链路验证（2026-08-14 通过）：scan→pull→claim(agent_id 反查=dsh)→重复 claim 409→ack→DB 溯源 consumed_by+signal_acks 全链路 OK（tmp/verify_e2e_signal2.py） |
| T-63 | Task | WebUI 审查修复（信号消费前端 + wiki 写入 + agent_model + 统一检索入口） | ST-7 | ✅ 已解决 | — | P1-1 信号消费前端（roles.ts 加 ackCareSignal + CareSignal 补 consumed_by + SignalsView 认领/回执交互 + hint 更新）；P2-1 wiki 写入（wiki.ts createWikiPage + WikiView 新建入口）；P2-2 agent_model（admin.ts registerAgent + AgentsTab 表单）；P2-3 统一检索入口（MainLayout 侧边栏加 /search）；P3-1 AGENT_KEY_PATHS 覆盖 care 端点；P3-3 总览命名统一；P3-5 主题 system 模式。**已完成（2026-08-14）**：8 文件改动，npm run build 通过（858ms），全部修复项落地 |
| ST-31 | Story | 通信渠道兜底铁律写入接入纪律（当前会话兜底，直到用户取消） | ✅ 已解决 | v1.1 | 背景：用户定（2026-08-14）「当前会话是兜底通信渠道——任何主动消息（关怀/提醒/告警等），不管发不发其它通道（微信/飞书/Telegram 等），当前会话都来一条，直到用户取消」。查证确认现有接入纪律（五条铁律 + 事件对接）无此条。AC：①self_config 模板加「通信渠道」兜底段②AGENTS.md/README 中英/agent-onboarding §4.2 同步③测试断言补关键词。触发词：2026-08-14 用户「hermes 的事你别管了，只记得提醒接入 agent，当前会话是拖底通信渠道」。**已完成（2026-08-14，T-67）**：mcp_server.py self_config 模板 + AGENTS.md + README 中英 + agent-onboarding §4.2 加「通信渠道」兜底铁律；test_mcp_server self_config 断言补兜底通信渠道/微信/飞书/Telegram；test_mcp_server 25 passed；变更记录 B62 |
| T-67 | Task | 接入纪律补「通信渠道兜底铁律」（当前会话兜底） | ST-31 | ✅ 已解决 | — | self_config 模板 + AGENTS.md/README 中英/agent-onboarding §4.2 加通信渠道兜底段；test_mcp_server 断言补关键词 |
| ST-30 | Story | 事件对接写入接入纪律（SSE 长连 + 游标拉取 + 短连 pull 三接法） | ✅ 已解决 | v1.1 | 背景：主动关怀要「主动」，靠「对话开始 signal_pull」不够——等用户开新会话才拉信号，关怀退化为被动响应。查证发现 SGME 早有完整事件订阅机制（`GET /v1/events/stream` SSE 长连 + Last-Event-ID 断线补偿 + `GET /v1/events/pull` 持久游标 + `signal_subscribers` 表），但接入纪律五条铁律只字未提，agent 接入后不知道。AC：①铁律第 4 条升级为「事件对接双模式」（常驻 agent 首选 SSE，无常驻用 signal_pull）②接入纪律补「事件对接」段（事件三类 care_*/memory_updated/anomaly_warn + 三种接法 + 端口/Key 说明）③self_config 模板 + AGENTS.md + README 中英 + agent-onboarding 同步。触发词：2026-08-14 用户「纪律条款有教 agent 创建定时任务吗 / 定时关注信号 / 直接对接事件吗」。**已完成（2026-08-14，T-66）**：mcp_server.py self_config 模板铁律 4 升级双模式 + 新增「事件对接」段；AGENTS.md/README 中英/agent-onboarding.md 同步（含 §4.1 事件对接表）；test_mcp_server.py self_config 断言补 SSE/pull/subscriber_id/Last-Event-ID/事件三类关键词；test_mcp_server+test_care 61 passed；变更记录 B61 |
| T-66 | Task | 接入纪律补「事件对接」（SSE 长连/游标拉取/短连 pull 三接法） | ST-30 | ✅ 已解决 | — | self_config 模板铁律 4 升级双模式 + 事件对接段；AGENTS.md/README 中英/agent-onboarding §4.1 同步；test_mcp_server self_config 断言补事件关键词 |
| ST-29 | Story | 角色模板对 agent 可见可调（MCP 角色工具 + 五条铁律） | ✅ 已解决 | v1.1 | 背景：角色模板（roles/butler/companion/friend/mentor.json + assemble 装配）已存在但只暴露在 HTTP API 层——MCP 工具集无角色工具、agent_onboarding 的 self_config 模板不提角色，接入的 agent「不知道角色存在、想调也调不了」（2026-08-14 用户实测：四角色关怀全靠手动查文件+调 HTTP 端点）。AC：①MCP 加 role_list（发现）/ role_assemble（调用人设）/ role_active_get/set（记录当前角色）②ONBOARDING_TOOLS 同步（23→27）③self_config 模板 + README/AGENTS/onboarding 纪律四条→五条（第 5 条：会话开始 role_list 看角色 → role_assemble 按其说话）④换皮不换芯（角色=沟通外皮，记忆池不动）。触发词：2026-08-14 用户「怎么让接入的 agent 知道并调用角色模板，靠什么保证」。**已完成（2026-08-14，T-65）**：mcp_server.py 加四工具（复用 operations.care 的 list_roles/assemble/get_active_role/set_active_role，入口层只做协议翻译）+ ONBOARDING_TOOLS 23→27；self_config 模板加第 5 条铁律 + AGENTS.md/README 中英/agent-onboarding 四条→五条；test_mcp_server.py 工具集断言 +23 工具 + role 四工具闭环测试；test_mcp_server+test_care 61 passed、mcp_wiki+signal 22 passed；变更记录 B60 |
| T-65 | Task | MCP 角色工具 + 五条铁律（角色模板对 agent 可见可调） | ST-29 | ✅ 已解决 | — | mcp_server.py 加 role_list/role_assemble/role_active_get/role_active_set（复用 operations.care，协议翻译）；ONBOARDING_TOOLS 23→27；self_config 模板 + AGENTS.md/README 中英/agent-onboarding 四条→五条铁律（第 5 条角色纪律）；test_mcp_server.py 工具集断言 + role 四工具闭环测试 |
| ST-28 | Story | 主动关怀闭环：关怀信号自动产生（扫描挂入 Dream 定时器） | ✅ 已解决 | v1.1 | 背景：ST-27 已闭环「消费端」（pull/claim/ack 三层 + TTL 归档），但「信号产生端」存在断点——`care/signals.py` 注释声称「与 Dream 协同定时扫描（首次手动触发拉起定时器）」，实际代码无定时器：`scan_care_signals` 仅有 HTTP `POST /v1/admin/care/scan`（手动）与 `care_consumer.py`（需配 cron）两个调用点，Dream 定时器只接了 `purge_expired_signals`（清理）未接 `scan_care_signals`（产生）。后果：无人手动 scan/配 cron 时关怀信号永不产生，agent `signal_pull` 永远拉空 → 主动关怀不发声。AC：①Dream 生命周期加 care 扫描阶段（信号自动产生，幂等去重）②受 care.enabled 控制（与 routes_care 挂载同开关）③独立容错（扫描失败不阻塞 Dream 其余阶段）④stats/日报/日志暴露 care_signal_count。触发词：2026-08-14 用户「以目前功能主动关怀能实现吗」→ 查证发现扫描无定时器断点。**已完成（2026-08-14，T-64）**：dream.py 生命周期 ③ 加 care 扫描阶段（紧接信号 TTL 归档，`scan_care_signals` 零 LLM 幂等）+ care.enabled 开关 + 独立容错 + care_signal_count 进 stats/summary/日报/日志；test_dream.py +3 用例（扫描触发产生 care_daily 信号 / enabled=false 跳过 / 扫描失败继续）；test_dream+test_care 57 passed、signal 11 passed；变更记录 B59 |
| T-64 | Task | Dream 定时器接入关怀信号扫描（主动关怀自动产生信号） | ST-28 | ✅ 已解决 | — | dream.py 生命周期加 care 扫描阶段（受 care.enabled 控制 + 独立容错 + stats/report/日志加 care_signal_count）；test_dream.py +3 用例（扫描被触发 / enabled=false 跳过 / 阶段失败继续）；pytest test_dream+test_care 57 passed + signal 11 passed；变更记录 B59 |
| T-68 | Task | 提炼成本治理：prescreen fallback 熔断 + 动态链继承采样参数（修复 DSH 会话提炼空 content） | ST-1 | ✅ 已解决 | — | 背景：搬家后 SGME 账单占比过高（08-16 ¥10.65/¥19.80=53.8%）。根因①embed 不可达时 prescreen 回退全量召回（单次 l1_conflict 最高 87 万 tokens，08-11/12 单日 9800 万 tokens 元凶）；根因②T-43 动态链 `_build_node` 只复制 providers 连接字段、丢失 llm.yaml 静态节点的 `extra_body: thinking disabled` → 带 agent_model 的 DSH 会话（2456ee64）连续 12 次 L1 error（思考模型输出 reasoning_content、content 为空）。改动：①`l15.prescreen.fallback` 新增（默认 full_recall 向后兼容，生产 skip_conflict=embed 不可达跳过冲突检测直接 store 零 LLM）；②`resolve._build_node` 从静态链同 provider 节点继承 max_tokens/sampling/extra_body；③生产 sgme.yaml 设 fallback: skip_conflict。测试：test_l15_prescreen +4、test_llm_resolve +4，相关 170 用例全绿。真实链路验证（2026-08-16）：2456ee64 从连续 error → 一次提炼成功（增量 130 条/记忆 40 条 + L1.5 正常裁决）；容器重启 healthy。变更记录 B65 |
| T-69 | Task | 记忆去重治理：content 重复清理 + memory_sources 唯一约束 | ST-1 | ✅ 已解决 | — | 背景：全库 11634 条中 73 组 content 重复（161 条 active，08-06 L1.5 上线前遗留）；memory_sources 无唯一约束（历史同源挂 271 条）。改动：①scripts/dedup_memories.py（dry-run/apply + 备份，归档重复保留最新，原件不删）②memory_sources 加 UNIQUE(memory_id, source_ref) 复合主键（db.py DDL）+ insert 改 INSERT OR IGNORE（memory_dao.py）③scripts/migrate_sources_unique.py（SQLite 重建表迁移，可重入 + 数据去重 + 备份）。测试：test_storage.py +2（约束存在 + 同源重复拦截）。真实执行（2026-08-16 NAS）：dedup 归档 88 条保留 73 条 → 重复 active 组数 73→0；迁移后 memory_sources 11548 行、PK 生效；同源多记忆（271 条）验证为内容各异合法记忆；容器重启 healthy + 提炼冒烟通过（L1.5 store=4 merge=4）。变更记录 B66 |
| T-70 | Task | Docker 部署固化：Dockerfile 合入 git + 镜像 commit 固化修复 + NAS 部署模板 | ST-1 | ✅ 已解决 | — | 背景：T-68/T-69 的代码修复经 docker cp 注入运行中容器（可写层），镜像本身无修复——容器重建即丢（假部署风险）；B64 遗留「git 安装入主 Dockerfile 推迟到下次镜像更新」。改动：①Dockerfile 加 git 安装（apt install git + safe.directory，单一入口）②docker commit 固化运行中容器 → sgme:1.0.0b1-nas-git-t69（含全部修复，验证 l15/resolve/db/memory_dao 标记齐全）③NAS compose 指向 t69 镜像 ④tmp/nas-docker-compose.yml 部署模板入 git（NAS 生产 compose 真相源）。验证：t69 镜像代码含全部修复标记；NAS docker.env 密钥完整；容器 healthy + 提炼冒烟通过。变更记录 B67 |
| T-71 | Task | dshfind 可安装判定修复：根 package.json 瘦包装（dsh-sgme）+ README 安装段 | ST-26 | ✅ 已解决 | v1.1 | 背景：dshfind.com 判定 freehul/sgme「不是可安装插件包」（仓库根无 package.json，dsh plugin add github: 必挂）——实际 dsh-sgme 插件（adapters/dsh/sgme-bridge/）已发布 npm v0.1.1（2026-08-14），dshfind 只看仓库根 manifest 导致误判。改动：①根 package.json（name=dsh-sgme + dsh.bundle→bridge cordis.patch.yml + main→已提交 lib/index.js，peer 依赖与 bridge 一致，prepublishOnly 防误发布覆盖真实 npm 包）——git 直装可加载 + dshfind 推导 npm 安装命令 ②README 中英加「DSH 插件安装」段（主推 npm 一条命令）③变更记录 B68。**已完成（2026-08-16）**：根 package.json 创建 + README 中英安装段 + 变更记录 B68；验证：JSON/路径/依赖一致性 ✓、adapters/dsh pytest 17 passed、dsh 临时 profile link 安装冒烟——reconcile 进 bundles + dump-config 挂载 id:sgme ✓（附：pnpm 9.x fresh profile 撞 ERR_PNPM_ADDING_TO_ROOT 为环境坑，任意插件必现，排障见 adapters/dsh/README.md 复现坑位） |
| T-72 | Task | Docker 新用户开箱修复：多阶段 WebUI 镜像 + 首次启动物化 sgme.yaml + runbook Docker 章节 + NAS 全新构建验证 | ST-1 | ✅ 已解决 | v1.1 | 背景：核查 Docker 部署（2026-08-16 用户问「用户 docker 安装不会出问题吧」）发现 4 缺口：①git Dockerfile 从未全新构建验证（B67 遗留：NAS 生产镜像是 docker commit 固化，非 Dockerfile 构建）②SGME_HOME=/data 时运行时只读 $SGME_HOME/config/sgme.yaml，镜像内 config/sgme.yaml 不加载——空卷启动=全默认（l15 prescreen 默认关 + fallback full_recall，防烧钱 skip_conflict 丢失）③WebUI 不进镜像（Dockerfile 无 ui/，.dockerignore 排除 ui/dist），compose 注释却写「HTTP API + WebUI」误导 ④runbook 无 Docker 章节、NAS 拉取链路未接（/vol1/1000/git/sgme.git bare 仓空、无 remote、无 cron）。改动：A 多阶段 Dockerfile（node:20-alpine Stage 1 构建 ui/dist 入镜像 + python:3.11-slim Stage 2）B docker/entrypoint.sh 首次启动物化默认 sgme.yaml 到 $SGME_HOME/config/（生产调优 prescreen+skip_conflict）C runbook §16 Docker 部署章节 D NAS 拉取链路接通（bare 仓接 gitee remote + fetch）E NAS 全新构建 + 空卷启动冒烟（health/WebUI/物化 sgme.yaml）。**已完成（2026-08-16）**：多阶段 Dockerfile（node:20-alpine WebUI + python:3.11-slim）+ docker/entrypoint.sh 物化 + runbook §16 + NAS bare 仓接 gitee remote（HEAD→main）；NAS 全新构建 sgme:1.0.0b1-git-e2e 成功（BUILD_EXIT=0），空卷冒烟全绿（/v1/health 200、sgme.yaml 物化、WebUI HTML、entrypoint 日志），生产容器 t69 未动；变更记录 B69 |
| T-73 | Task | 生产容器切换到 git 构建镜像（sgme:1.0.0b1-git-t72）+ 全链路测试 | ST-1 | ✅ 已解决 | v1.1 | 用户指令（2026-08-16）：接入 SGME 的 agent 只有当前会话在工作，由本 agent 完成容器重建并测试。前置：手动备份（nas_backup.sh OK）+ 确认 /data/config/sgme.yaml 存在（md5 4e409685…，生产调优版）防物化覆盖。改动：①NAS src 同步 209d926 → 构建正式镜像 sgme:1.0.0b1-git-t72（缓存层秒级）②NAS compose 备份 docker-compose.yml.bak-pre-git-t72 + image 切换到 git-t72 ③docker compose up -d 重建（数据卷 bind mount 不动）④测试（见 B70）。**已完成（2026-08-16）**：容器 sgme:1.0.0b1-git-t72 Up (healthy)；全链路测试通过（B70）
| ST-32 | Story | wiki 渐进式披露共享知识库改造（技能手册/经验统一入 wiki_pages，L1 常驻 + L2 按需，自进化写回） | 灵感：progressive-skill + 多 agent 共享 | ✅ 已解决 | v1.1 | 设计决策见 docs/design/SGME-wiki渐进式披露共享知识库改造方案-v0.3.md（v0.1 送审稿存档同目录）；三路调研见 D:\Projects\AIRDT\docs\research\wiki-kb-benchmark\；设计决策已入 wiki（NAS 9910，page_id wiki渐进式披露共享知识库-设计决策-v0-1-45810444） |
| T-74 | Task | W1 数据模型：wiki_pages 加 description/description_seg/author/status/supersedes + FTS 扩展 description_seg（含触发器升级修复） | ST-32 | ✅ 已解决 | v1.1 | db.py WIKI_DDL 5 列 + _migrate_wiki_page_columns（PRAGMA 幂等自愈）；fts.py 扩 description_seg + _triggers_have_description 重建触发器；wiki_dao 参数贯通；operations _LIST_SKIP_FIELDS；tests/test_wiki_columns.py 7 用例。提交 bceb792 + 8702e5b（触发器遗漏修复） |
| T-75 | Task | W2 检索语义：统一搜索过滤 skill 标记页 + superseded 全路径过滤 | ST-32 | ✅ 已解决 | v1.1 | fts.py 两路径（BM25+LIKE）status='active' + tags 携带；search.py _is_skill_page 精确判断（Python 层防脏数据）；list_pages 过滤 superseded；tests/test_wiki_filter.py 5 用例。提交 d0c618e |
| T-76 | Task | W3 写回接口：PATCH /v1/wiki/pages/{id} + MCP wiki_page_update（append ADD-only + entry hash 幂等） | ST-32 | ✅ 已解决 | v1.1 | operations/wiki.py update_page（append 默认带来源+hash 标记，入口查重 noop，description 默认不动）；routes PATCH；mcp wiki_page_update + ONBOARDING_TOOLS；tests/test_wiki_update.py 10 用例。提交 b8b8773 + 8702e5b（create 链路 description 打通） |
| T-77 | Task | W4 自进化管线：独立第三条管线（会话→经验→写回手册），费用门禁 + 规则闸门 + 独立游标 | ST-32 | ✅ 已解决 | v1.1 | operations/evolve.py（门禁 min_rounds → LLM 提炼 → 闸门 → append/create → 审计）；evolve_dao.py wiki_evolve 表（与 refine_cursor 分离）；prompts/evolve_extraction.txt；POST /v1/wiki/evolve/trigger + MCP wiki_evolve_trigger；tests/test_wiki_evolve.py 6 用例。提交 d4d45c6 |
| T-78 | Task | W5 bridge 工具：wiki_pages/wiki_page 暴露给 DSH + 自进化自动触发 | ST-32 | ✅ 已解决 | v1.1 | tools.ts 加 wiki_pages（按 category 列目录）/wiki_page（拉全文）；sgme-client wikiListPages/wikiGetPage/evolveTrigger；session-sync turn/end 后自动触发 evolve（evolveEnabled 默认 true）；tests 9 用例（wiki-tools 7 + session-sync 2）。提交 5fb9e3f + 2224470 |
| T-79 | Task | W6 索引 skill：wiki-skill-discovery（L2 索引层，搜索引导）+ install 部署 | ST-32 | ✅ 已解决 | v1.1 | 源 adapters/dsh/skills/wiki-skill-discovery/SKILL.md（随 git）；install.py deploy_index_skill 幂等部署到 ~/.agents/skills/；DSH watcher 实时感知（catalog 已含）。提交 a310c39 |
| T-80 | Task | W7 手册入库 + NAS 生产部署 | ST-32 | ✅ 已解决 | v1.1 | SGME操作手册（category=skill/sgme，完整版含 description，page_id sgme操作手册-749c4590，旧版 superseded）；NAS 部署：push → pull → docker build sgme:1.0.0b1-wiki-v1 → 容器替换 → 生产验证 10/10（迁移字段/PATCH 幂等/FTS 命中 description/统一搜索过滤/evolve 冒烟）。变更记录 B71 | |
| T-81 | Task | W4 缺陷修复：自进化管线 cfg 空配置导致 LLM 必炸（P0） | ST-32 | ✅ 已解决 | v1.1 | 背景：routes.py/mcp_server.py 两入口把 {} 传给 evolve_trigger，call_with_fallback 读 cfg["chains"] 必抛 ValueError("未知链名: refinement")，自进化永远产不出经验条目。根因更深一层：chains 在 cfg["llm"]["chains"] 而非顶层，与 tier0/l2/care 各管线传 cfg["llm"] 的约定不符。改动：两入口改传 cfg["llm"]（HTTP request.app.state.cfg["llm"] / MCP _app_state.get("cfg", {}).get("llm")）。tests/test_wiki_evolve.py +2 用例（单元透传 + HTTP 端点透传）。变更记录 B72 |
| T-82 | Task | W3 缺陷修复：create_page 补 supersession 判等取代（P1） | ST-32 | ✅ 已解决 | v1.1 | 背景：设计 §5.1 定义 create 时同 category+title 的 active 旧页 content 不同 → 旧页置 superseded，但 create_page 只做 upsert 无判等，status/supersedes 成死字段，同题不同内容并存两个 active 页。改动：wiki_dao 加 find_active_same_title/mark_superseded；create_page not exists 时查同 title+category 旧页并标记。tests/test_wiki_supersession.py 6 用例。变更记录 B72 |
| T-83 | Task | W5 缺陷修复：bridge 补 wiki_page_update 工具（P1） | ST-32 | ✅ 已解决 | v1.1 | 背景：设计 W5 要求 3 工具（wiki_pages/wiki_page/wiki_page_update），tools.ts 只实现前两个，SKILL.md 要求回写但 DSH 侧无工具。改动：sgme-client 加 patch/wikiUpdatePage；tools.ts 加 createWikiPageUpdateTool 并注册；wiki-tools.test.ts +3 用例；重建 lib/index.js。变更记录 B72 |
| T-84 | Task | bridge 类型声明路径修正：package.json types 指向 lib/index.d.ts + build 去冗余 tsc | ST-32 | ✅ 已解决 | v1.1 | 背景：tsdown.config.ts dts:true 生成扁平 lib/index.d.ts，但 package.json types/exports.types 指向不存在的 lib/types/index.d.ts（tsc declarationDir 产物被 tsdown clean:true 清掉属死产物）。改动：package.json types→lib/index.d.ts、exports.types→./lib/index.d.ts、build 脚本 tsc&&tsdown→tsdown；tsconfig.build.json 加废弃注释保留备查。验证：pnpm build 单步生成 lib/index.d.ts + lib/index.js。变更记录 B73 |
| T-85 | Task | evolve 参数命名澄清：cfg 重命名 llm_cfg（实为 llm 段） | ST-32 | ✅ 已解决 | v1.1 | 背景：evolve._llm_call/evolve_trigger 参数名 cfg 实为 llm 段（load_config()["llm"]），命名误导是 T-81 P0 的深层成因。改动：参数 cfg→llm_cfg（含 docstring 澄清），外部位置参数调用零影响。验证：test_wiki_evolve.py 全绿。变更记录 B73 |
| T-86 | Task | bridge 对齐 MCP 新工具：三池登记 + 角色装配 + 记忆纠错（9 工具） | ST-26 | ✅ 已解决 | — | 背景：MCP 侧已长出三池（idea_add/demand_create/project_register）+ 角色（role_list/role_assemble/role_active_get/role_active_set）+ 记忆纠错（memory_get/memory_reject）共 9 个工具，bridge 只挂 9 个检索/信号工具，B81「待办主动登记」在 dsh 侧无工具可用。改动：sgme-client.ts 补 put helper + 9 方法；tools.ts 加 9 工具并注册（18 个） + dimensions 描述去 projects/tasks（B81 维度裁剪）；install.py AGENTS.md 模板同步 + 升级锚点 demand_create；顺手修 e784b80 遗留 typecheck 3 处。**已完成（2026-08-18）**：vitest 131 passed（含新增 admin-tools 20 用例）/ typecheck 0 错误 / pytest test_install 7 passed / NAS 生产端点真实冒烟 9/9（scripts/oneoff/verify_t86_endpoints.py）。变更记录 B82 |
| T-87 | Task | 信号批量清空/管理功能缺失（SSE 接入前历史信号堆积无官方清理通道） | ST-27 | ✅ 已解决 | v1.0 | 2026-08-20 consume_all 端点+MCP signal_clear+WebUI 全部消费（50 测试），B90 | 背景：2026-08-20 启用 dsh SSE 事件订阅前，历史信号堆积（memory_updated/anomaly_warn 等）无官方批量清理入口——T-41 关怀信号面板只有 scan/列表/单条 consume；admin 无批量清空端点；TTL 归档（T-57）只按时间清理，无法立即清空。临时绕行：循环调用 GET /v1/events/pull 推进订阅者游标（2026-08-20 实测消费 28 条）——属 API 游标语义的合规用法，但用户指出这是功能缺失，应提交问题而非绕路。AC：①新增批量消费/清空端点（如 POST /v1/admin/events/consume_all 或 /v1/admin/signals/clear，按 subscriber_id 或全局，支持按 type 过滤）②WebUI 关怀信号面板补「全部消费/清空」按钮（T-41 SignalsView）③MCP 同步补工具（如 signal_clear）④测试 + 变更记录；触发词：2026-08-20 用户「信号模块没有相关功能，说明是功能缺失的问题，要提交问题，而不是直接操作数据库」
| T-88 | Task | DSH 注入对话内容驱动改造（首句选场景 + inject 工具） | ST-26 | ✅ 已解决 | v1.0 | 2026-08-20 用户定：注入=对话内容驱动（对齐 Hermes T-42）；DSH 侧 context.ts 首步改为首句 search(memory+wiki) 命中 L2 场景注入、无命中回退模板；tools.ts 加 inject 工具；缓存红线——不做每轮动态注入（DeepSeek 前缀缓存 50 倍价差，动态注入输入成本 ×14） |
| T-89 | Task | search 召回去重 + limit 截断（重复记忆稀释注入） | ST-5 | ✅ 已解决 | v1.0 | 2026-08-20 实测注入 10 条记忆 4 对重复：search_memories RRF 融合后不截断 limit（两路各 limit 条→2×limit）+ 无内容去重（L1 重复落库全召回）；修复：融合后按 content 去重 + 截断 limit |
| T-90 | Task | dsh 历史会话导入落地（import_history.py 对齐 rc8 存储格式） | ST-26 | ✅ 已解决 | v1.1 | 2026-08-21 全流程完成（B97）：重写 discover_sessions（递归扫 `<workspace>/*/session.jsonl.zstd`，兼容老 session-<uuid>/ 与新 <uuid>/ 命名，排除 Temp/cli-test）+ parse_session_file（zstd 流式解压 + 事件流解析，字段对齐 session-sync.ts）+ 查重改查 NAS（`/v1/admin/sessions`，修复本地库恒空缺陷）+ append 429 退避重试；pyproject 补 zstandard；31 测试全绿；真实全量导入 141 会话 → 130 成功入库（209 含历史）、11 空会话跳过；提炼 8 error 为 LLM 链降级（drop_batch，非格式问题），batch_scan 兜底 | 背景：T-49 的 import_history.py 是占位骨架（discover_sessions 只扫顶层 *.jsonl，从未对接真实格式）。2026-08-21 实测确认 DSH rc8 真实存储：`~/.dsh/sessions/<workspace>/<会话id>/session.jsonl.zstd`（zstd 多帧压缩，首行 session 头 + 事件流 user/message、assistant/message、tool/result 等，字段对齐 session-sync.ts），老格式 `session-<uuid>/` 与新格式 `<uuid>/` 目录并存需兼容；rc8 release notes 明确存储格式不兼容（SQLite 性能优化 + 体积减小）。目标：重写 discover_sessions（递归扫 `<workspace>/*/session.jsonl.zstd`，兼容老/新命名，排除 Temp 测试目录）+ parse_session_file（zstd 流式解压 + 事件流解析）+ pyproject 补 zstandard 依赖 + 测试更新 + B97 |
| ST-34 | Story | 自动检测新版本并引导用户更新（检测→提示→确认→自动更新） | ✅ 已解决 | v1.0.0b4+ | 灵感：SGME 无任何版本检测/更新引导（2026-08-20 盘点确认），用户需手动看 GitHub release + runbook 升级。AC：①服务端定时检测 GitHub Releases API（GitHub 优先），与当前版本语义化对比，失败静默降级不拖垮服务 ②health 端点只增字段（update_available/latest_version/update_checked_at/update_error），向后兼容 ③WebUI 检测到新版显示提示条 +「立即更新」确认弹窗（链接 release 页）④Docker 形态：用户确认后经「意图文件 + 主机侧 cron 更新代理」自动完成更新（容器无特权、失败自动回滚旧镜像）⑤测试 + 文档 + 变更记录。决策（2026-08-21）：执行机制=意图文件+主机侧 cron 更新代理（用户确认）；检测源=GitHub 优先；范围=检测+提示+确认后自动更新（MCP 工具/信号后续独立 Task）。**完成（2026-08-22）**：T-91/92/93/95 ✅（v1.0.0b4+）+ T-94 脚本 NAS 部署完成（root cron 已配）+ 端到端复验通过（意图文件写入→脚本识别已是最新→清请求 exit 0）；NAS 生产容器无特权、自动更新链路全通 |
| T-91 | Task | 服务端版本检测模块（GitHub Releases API + semver 对比 + 静默降级） | ST-34 | ✅ 已解决 | v1.0.0b4+ | 2026-08-21 完成（B99）：新建 sgme/operations/update_check.py——GitHub Releases API（releases/latest 免 token）解析 tag_name + 语义化对比（预发布 b4<b5 视为新版、正式版>预发布）；网络/API 失败静默降级（记录 update_error 不拖垮服务）；模块级缓存 get_cached/refresh（health 高频读不重复请求外网）；config/sgme.yaml 加 update_check 段（enabled/interval_hours/source 默认 github）；13 测试全绿 |
| T-92 | Task | health 端点扩展（只增字段，向后兼容） | ST-34 | ✅ 已解决 | v1.0.0b4+ | 2026-08-21 完成（B99）：GET /v1/health 增加 update_available/latest_version/update_checked_at/update_error 4 字段（只增不改既有字段）；MCP health 契约冻结不动；启动（lifespan）+ 定时检查（update_check_task 复用 interval_hours）；顺带修复 test_stall_watch 历史遗留缺 model_config 断言；44 测试全绿 |
| T-93 | Task | WebUI 提示条 + 确认更新弹窗 + 状态轮询 | ST-34 | ✅ 已解决 | v1.0.0b4+ | 2026-08-21 完成（B99）：后端新增 sgme/operations/update_request.py（意图文件原子写 tmp+os.replace）+ POST/GET /v1/admin/update/request（admin 鉴权）；前端 DashboardView 健康卡片——发现新版高亮提示条（查看更新说明→release 页）+「立即更新」window.confirm 确认弹窗（说明备份/重建/回滚保障）+ 提交后状态反馈（updateStatus/updateMsg）；6 测试全绿 + npm run build 通过 + 真实冒烟 smoke_update_st34.py（health 4 新字段 + 意图端点 200 + request.json 原子落盘） |
| T-94 | Task | 主机侧更新代理 scripts/sgme-host-updater（含回滚） | ✅ 已解决 | v1.0.0b4+ | 2026-08-21 脚本+文档完成（B99）：scripts/sgme-host-updater.sh——NAS 主机 cron 每 5 分钟轮询 $DATA_DIR/update/request.json → 校验 target_version 格式（防注入）→ 执行 runbook 16.4 链（git pull → docker build 新镜像 → 备份 compose → 换 tag → compose up -d → 健康验证）→ 成功清请求 / 失败自动回滚旧镜像 + 标记 failed；锁文件防并发；bash -n 通过；容器保持无特权（不挂 docker.sock）。**NAS 部署完成（2026-08-22 复验）**：脚本已落位 /vol1/1000/Docker/sgme/scripts/（md5 与本地一致 95ff0317）、root cron 已配（*/5 * * * * → updater.log）、8-20 首跑识别"已是 v1.0.0b4"正常；本次端到端复验：写入 pending 意图文件 → 脚本识别当前镜像已是目标版 → 清请求 + exit 0，链路全通 |
| T-95 | Task | 测试 + 文档 + 变更记录（B99） | ST-34 | ✅ 已解决 | v1.0.0b4+ | 2026-08-21 完成（B99，注意 B98 被并行会话占用）：pytest 全绿（test_update_check 13 + test_update_request 6 + health 契约 + stall_watch 15，共 40+）+ npm run build 通过 + 真实冒烟 smoke_update_st34.py；runbook 16.4 新增自动更新章节（意图文件 + 主机代理 + cron + 回滚）；变更记录 B99（ST-34 全链路） |
| T-96 | Task | LLM 提炼免费链重构 + 新用户引导同步（agnes 主位三免费兜底） | ST-6 | ✅ 已解决 | v1.0.0 | 2026-08-22 完成（B100）：①降级链重构——agnes（agnes-2.5-flash，当前 $0/1M token，实测 1-4s）主位 → siliconflow（DeepSeek-V4-Flash 免费 1-3s）第二 → zhipu（glm-4.7-flash 永久免费但慢 38s/次 + 1305 风暴）末位兜底；原 zhipu 主 + deepseek 付费备用移除（降级链全免费化）②参数调优——max_retries 5→2（免费兜底就位快速切换）、退避 3s→60s、jitter 0.5s ③引导同步三处——docs/guide/免费模型Key申请指南.md 重写（三 Key 申请 + 链位表）、MODEL_KEY_MISSING_NOTICE + agent_onboarding requirement（mcp_server.py）文案、README 模型说明；架构 v0.9 §1/§24 + runbook §4.2/§16 对齐 ④版本 1.0.0b4 → 1.0.0 正式版（pyproject/__init__/health/app + 6 测试断言）⑤release-notes-v1.0.0.md 新建 |
| T-97 | Task | L2 场景聚合向量预筛 + 场景超限治理（active 276 > max 200 红警） | ST-5 | 🔴 进行中 | — | 背景：2026-08-22 巡检发现 L2 场景 active=276 超配置上限 200（红警每轮触发），根因=设计天花板——L2 每次只把**最多 50 个场景摘要**喂 LLM，超出部分不可见，merge 收敛跟不上 create 增长。目标：对齐 T-25（l15.prescreen）模式给 L2 加**场景级向量预筛**——按本批记忆向量 Top-K 召回相似场景再裁决，替代固定 50 摘要；超限治理（场景批量合并/归档）待预筛落地后评估。方案待列清单确认（2026-08-22） |

---

## 设计文档索引（依附关系）

| 设计文档 | 解决的需求 | 解决的问题 |
|---------|-----------|-----------|
| SGME-架构设计-v1.0.md | ST-1~ST-6 总纲；ST-1,ST-4,ST-5（接口契约）；ST-1~ST-5（数据模型）；ST-2（提示词/归一化）；ST-5（模板）；ST-6（LLM降级链） | T-1~T-4 裁决；专项（Dream/SkillsHub/创意池/模块化/备份/自动更新/Care Engine/Docker/三池/WebUI）见 §30 |
| SGME-L0文件格式-v0.1.md | ST-1 | — |
| SGME-评测基线-PRD-v0.1.md | ST-6（质量评测） | #32 评测集设计 |
| SGME-评测框架设计-v0.1.md | ST-6（质量评测） | #32 评测框架 |
| SGME-实施变更记录-v0.9.md | 全部（B 系列） | 实施记录兼运维手册 |
| SGME-WebUI设计-v0.1.md | ST-7（4 导航 22 视图 + 创意池 UI） | WebUI 技术栈/信息架构/视图明细/升格联动决策（2026-08-12） |
| （ST-34 设计决策内联于 Backlog 行） | ST-34 | 自动更新=意图文件+主机侧 cron 代理（2026-08-21 决策，容器无特权可回滚） |

---

## 维护规则

1. **立项即建**：新项目创建本文件（Backlog），后续一直存在
2. **需求来源登记**：新 Story 标注来源（灵感/对标/痛点），Epic 标注方向
3. **拆解**：Story 成熟后拆成 Task/Bug（拆解 = 设计分析的一部分），全部 Task 满足 AC = Story ✅
4. **设计依附**：设计文档声明解决哪些 Story/Task → 反向登记到索引表
5. **完成即标记**：✅ + 版本，**不删除条目**；commit/PR 引用编号（Closes ST-x / T-x）
6. **未解决即任务**：🔴 = 当前任务池；🟡 = 暂缓；✅ = 已解决
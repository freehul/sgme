# pre-push 密钥扫描白名单（每行一个 grep -E 正则；命中且在名单内 → 放行）
# 仅允许"测试/示例用假串"模式，禁止写入真实 key（2026-09-13 T-164）
#
# 说明：低熵占位（如 sk-000…0，去重字符 ≤ 3）由钩子自动放行，无需列此。
# 新增测试样例时：优先用低熵占位；确需高熵假串时追加到下面并注明出处。
sk-abcdefghij[A-Za-z0-9]*
sk-1234567890[A-Za-z0-9]*
